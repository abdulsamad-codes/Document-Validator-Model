# Project Context – FinTech Document Verification System

<!-- git identity fix verified 2026-08-22 -->

Last updated: 2026-08-22. This file is the single source of truth for project state across sessions/tools — keep it current rather than trusting an earlier assistant summary in chat history.

## 📍 Session picks up here (2026-08-22 end-of-day) — read this first

Everything below is re-verified against actual repo state today (`git log`, `git branch -a`, live commit contents), not carried forward from an earlier chat's memory — a prior triage note in this same session had gone stale on several points (see the correction at the bottom of this section) and that mistake shouldn't repeat.

**Done today, in order (all on `main`, all pushed):**
1. `organization_name` garbage-capture fix on a line-wrapped "on behalf of the" clause (`6759c21`, closed `3b1cd41`).
2. Tripartite `party_1link` regression fix — excluded Authority Letter preamble from the match (`6c26f8d`, closed `110f961`).
3. `BLUR_THRESHOLD` false-positive fix — global blur variance replaced with tiled 95th-percentile (`5e20a81`, closed `ee6aeb1`).
4. `audit/backend-verification-2026-08-20` merged into `main` (`904ab84`); branch deleted both local and remote (absent from `git branch -a`).
5. Modal focus-trap fix cherry-picked (`9c28012`, adds focus traps to `SessionTimeoutModal`/`ConfirmDialog` + an environment startup log warning). Note: `4f8c390` (a stale `revenue_services_listed` BRD-test-assertion fix) landed adjacent to this in the log but is an unrelated, independent fix — not actually tied to the focus-trap change despite landing close together.
6. `BilateralAgreementExtractor` rewritten against real content and real-sample validated for the first time ever (`fc27a24`).
7. `CrossAccountHolderRule` unregistered for `BILATERAL_AGREEMENT` — `account_holder` is a structural honest-miss, matching the `CrossBranchCodeRule` precedent (`19a2e29`, fixtures updated `c56efee`).
8. Tripartite `branch_code` investigated and accepted as a documented Bucket B gap — no real sample with a filled branch code exists yet (`106f757`).
9. Login page background video asset committed, 41MB (`3230882`).
10. CONTEXT.md docs cleanup batch: stale header date, missing merge-resolution entry, 5 stale Known Gaps entries corrected, `FormalRequestLetterExtractor` documented (`7859d68`).
11. Repo-root `pytest.ini` added — fixes bare `pytest` crashing on collection of `backend/scripts/*.py` (`cb84b08`).
12. `feature/afsana-validation-logs` fully reconciled: 2 real pieces landed (`6c794c9` — `OTHER_SUPPORTING_DOCUMENT`/`BULK_UPLOAD` frontend catalogue entries; `e2ce416` — hyphenated date format support in `_parse_date`), the rest correctly identified as superseded or unsafe and dropped (see the dated entries later in this file for the full reasoning, including the misrouted-`ONE_LINK_LETTER` finding that caught one drop). Branch deleted both local and remote.
13. `feature/samad-doc-splitter` closed: one real bug recovered (`frontend/.env.example`'s active `VITE_API_BASE_URL` line, silently dropped in an earlier refactor, breaks the Vite dev-server CORS proxy — restored, `2dc9d5c`), the other 3 commits confirmed stale docs/config. Branch deleted both local and remote.
14. PyMuPDF AGPL licensing decision logged and closed (`49e9b62`) — see the section directly below.
15. `SCHEDULE_OF_CHARGES` removed from the required document checklist — `DocumentScheduleRule` was an unconditional, unsatisfiable `overall_status` blocker (any FAIL forces overall FAIL; zero real samples of this type exist in ~21 real files checked all session), not just a missing extractor. Its real content overlaps `BILATERAL_AGREEMENT`'s already-extracted Transaction Charges section (`6cd94ee`, decision documented `433ab2c`).
16. `app.validation` module (`ValidationTask`/`ValidationRun`/`ValidationLog`) deleted outright — confirmed zero real callers anywhere, superseded in practice by `app.operator_workflow` (real, live triage queue) + `app.human_verification` (final decision), which already form the two-stage workflow this module was scaffolded for but never wired into. 18 files removed, 7 edited, one new Alembic migration (`fa41896`). CLAUDE.md and CONTEXT.md both updated to document `operator_workflow` and this module's fate (`89dbcb4`).
17. AuditLog viewer decision investigated and closed — the "write-only, never read back" premise from the 2026-08-15 foundation audit was **wrong on the backend half**: `app/system_logs/` already implements a complete, tested, IT-role-gated read API (`GET /system-logs`, `GET /system-logs/{id}`) directly over `AuditLogRepository.search()`; nobody had ever built the frontend that calls it. Only the frontend was actually missing. Built and shipped: `services/systemLogs.js`, `hooks/useSystemLogs.js`, `pages/SystemLogs/SystemLogsPage.jsx` (filterable table + detail panel, mirrors `ApplicationHistoryPage`'s shape), new "System Logs" nav entry (`strictRoles: ['IT']`), route registration, 9 new unit tests plus updated role-gate coverage in `Phase1Routes.test.jsx`/`roles.test.js` (`9c33446`). Frontend suite: 108 passed (up from 97), 0 lint errors, build clean. See the corrected Known Gaps entry below for the full investigation.
18. `ImageEnhancer` dead code decision investigated and closed — deleted `backend/app/preprocessing/enhancer.py` outright. The decision had actually already been made and documented: `app/preprocessing/__init__.py`'s own docstring states "Image enhancement intentionally lives in `app.document_processing.utils` ... and is not duplicated here" — someone had already decided this file was superseded, the cleanup just never removed it. Git provenance confirms the shape: `enhancer.py` was added in `32171b8` (2026-08-12, Samad's original synchronous splitter + CV preprocessing), superseded the same week by Zarghuna's async `bulk_queue` redesign; the real, live equivalent (`document_processing/utils.py::preprocess_image`, deskew → denoise → CLAHE → adaptive threshold) actually predates it by a day (`726a607`, 2026-08-11) and runs on every real document via `processors.py`. Zero callers, zero tests, confirmed by fresh grep before deletion. See the corrected Known Gaps entry below.

**Correction to a stale in-session recollection**: earlier today's own triage note described items 12 ("afsana's remaining 3 commits — not yet reconciled"), 14 ("PyMuPDF — still open"), 15 ("Schedule of Charges — zero-sample gap still open") and CNIC_BACK ("still open") as outstanding. All four were already resolved by the time that note was written — re-verify against `git log`/`git branch -a` before trusting any in-session "still open" list, this file included, rather than propagating one forward.

**Still genuinely open** (re-verified against the current Known Gaps section below, not assumed):
- **CNIC_BACK** — not open as a *decision* (that was closed 2026-08-17: deliberately out of scope), but the standing condition (zero real `CNIC_BACK` samples exist) remains, so it stays unmapped until one turns up.
- Rule-engine coverage gaps vs. `docs/Master_Rules_Combined.md` (blank-date enforcement, PayMin/Digital Muhasil/Paymere BCX terminology, organization-name cross-document consistency, per-page 1-Link signature, layout/point-numbering conformance, E-Stamp/Notary Public visual checks, Formal Request Letter subject-line, CNIC expiry/completeness) — no extraction support exists for any of them yet; don't add a rule before extraction.
- Master Rules §3's AMC↔Tripartite/Bilateral branch-name consistency check has no implementing rule at all (found 2026-08-16).
- AMC `issue_date` gap — a bare `"Date"` label alternative fixes 2 real samples but breaks 2 others; not landed, no safe fix found yet.
- 1-Link Letter checklist-meaning mismatch (real samples are Participation Memoranda, not Section 4's actual required content) — flagged, needs a product/ops scope decision, not a code fix.
- `fcc9fda`'s blank-page technical-validation check causes the 12 pre-existing `test_document_analysis_api.py` failures (previously logged elsewhere in this file as "13" — today's own suite runs isolated the 13th as an unrelated environmental artifact, a stale local `worker.heartbeat` file, not a 13th named code failure; the real, stable code-level count is 12). Needs to be raised with Zarghuna, not touched reactively.
- Teammate bug triage: 28 confirmed issues in `docs/TEAMMATE_BUG_TRIAGE.md`, none fixed yet, pending an explicit scope decision.

## ✅ Legal/Compliance Decision: PyMuPDF Licensing (closed 2026-08-22)

`PyMuPDF` (imported as `fitz`) is used for PDF rendering/extraction throughout `document_processing` and `preprocessing`. The installed package is **dual-licensed AGPL-3.0 / Artifex Commercial License** — confirmed directly from the installed package's own metadata (`License-Expression`/`License` field), not from memory (foundation audit, Section 8). AGPL is the strongest copyleft license that exists: its network-use clause requires making the source code of the **entire application** available to any network users of this service, unless a commercial license has been obtained from Artifex.

**Decision (Abdul, 2026-08-22): staying on the free AGPL license, no commercial Artifex license.** Explicitly accepted: this means the full application source must be made available to anyone using the app over the network, per AGPL's network-use clause. Closed; no further legal review pending on this point.

## What this project is

A full-stack system for banks/KPITB to onboard merchants: upload financial onboarding documents, run them through OCR → field extraction → confidence scoring → normalization → a business-rule engine → human review, and curate corrected data into a versioned dataset for continuous learning. Backend: FastAPI + PostgreSQL (SQLAlchemy 2 + Alembic). Frontend: React 19 + Vite.

**⚠️ The GitHub repo (`abdulsamad-codes/Document-Validator-Model`) is public.** Real onboarding documents (real IBANs, CNICs, officer names) belong in `Confidential Data/` at the project root, which is gitignored — never loose in the repo. On 2026-08-13 a real onboarding PDF was found committed and pushed un-ignored (fixed by untracking + gitignoring it, commit `bc57cff`); git history was deliberately left as-is rather than rewritten, so that one file is still recoverable from an old commit if anyone goes looking. See `Confidential Data/` for the ~19 real documents already handled correctly.

**⚠️ Never run `pytest` while real data is loaded in the dev database.** `tests/conftest.py`'s autouse `isolated_database` fixture runs `DELETE FROM applications` (cascading to documents/queue_jobs) before AND after **every single test**, against whatever database `SessionLocal`/`DATABASE_URL` points at — which locally is the same Postgres database the running backend and queue worker use. On 2026-08-13 this wiped a real uploaded application while it was mid-processing (see below). Before running the suite locally, check the `applications` table is empty first, or point `DATABASE_URL` at a separate test database. (Independently confirmed by a parallel session working on `feature/zarghuna-bulk-queue`: a 10-minute full-suite run wiped all users mid-run, breaking login until reseeded — same root cause.)

**⚠️ This includes running two `pytest` processes at the same time, not just pytest + a live app operation.** Found 2026-08-16 (third collision of the day, after the destroyed application 8547 that morning): a background full-suite run was already in progress, and a second, separate `pytest` invocation was started to check a small set of new tests — both hit the shared dev DB via the same autouse `isolated_database` fixture, which wipes tables before/after *every* test regardless of whether that specific test touches the DB. This can produce a spurious failure in whichever run loses the race, indistinguishable from a real regression without isolating and re-running the failing test alone. **Mandatory pre-flight before running pytest, every time, no exceptions: `ps aux | grep python` first, confirm nothing else is already running against the same database** (another pytest process, a live upload/queue drain, a script using `SessionLocal`). Splitting-only or pure-OCR work that never touches the DB (e.g. `backend/scripts/ocr_cache.py`) is exempt — it's safe to run alongside pytest, only CPU contention applies there.

## Current state: everything merged to `main`, test suite green, Operator Dashboard + Validation Report + Human Review + Enterprise Settings (functional workspace preferences) + Processing page + Application History + Performance shipped

Three teammates' branches (`feature/samad-doc-splitter`, `feature/zarghuna-bulk-queue`, `feature/afsana-validation-logs`) are all merged into `main`. Work happens directly on `main`.

**Backend test suite: 709 passed, 0 failed as of the last measurement** (`cd backend && .venv/Scripts/pytest.exe -q`, reported to take up to ~10 min on some machines). **Re-verify the `applications` table is empty before running it** — see the pytest warning above — and re-run to confirm the number if it's been a while.

### What each branch shipped
- **Samad**: original bulk PDF splitter + CV preprocessing + bulk-upload endpoint (later superseded by the async redesign below).
- **Zarghuna**: async bulk upload — `upload_bulk` stores the raw PDF as a `BULK_UPLOAD`-typed placeholder `Document` and enqueues it on a Postgres-backed job queue (`app/bulk_queue/`, `FOR UPDATE SKIP LOCKED` claiming). A background `BulkQueueWorker` later splits/classifies pages (`DocumentSplitter.split_bulk_pdf`, PaddleOCR fallback for scanned pages) — this fixed the original synchronous-splitter HTTP timeout on large/scanned bulk uploads.
- **Afsana**: the `validation` module — `ValidationTask`/`ValidationRun`/`ValidationLog` models plus a full API (`app/validation/routes.py`): task lifecycle (create/start/complete/reject/request-correction), stored check results, immutable audit log, review-time field/evidence logging. This is the backend half of the human-review workflow.

### 2026-08-13 stabilization session (this session)

A prior session (different tool) had merged everything but left the suite at "89 failing tests, never root-caused," and had introduced a rule-registry regression while trying to fix an unrelated test. This session found and fixed the real causes:

- **Missing migration**: `DocumentType.BULK_UPLOAD` existed in Python but was never added to the Postgres enum — every bulk upload failed. Added `alembic/versions/02ffa030b9e1_add_bulk_upload_document_type.py`.
- **Wrong repository method call**: `upload_bulk` called a nonexistent `QueueJobRepository.enqueue()`; fixed to call the real `enqueue_uploaded_documents(...)`, removed a dead import, rewrote the stale docstring.
- **Real atomicity bug**: `DocumentRepository.create()` commits immediately, so a failure in the enqueue step right after couldn't be rolled back — fixed with an explicit compensating delete.
- **Added `DocumentSplitter.validate_structure()`**: a cheap synchronous readable/non-zero-page check so garbage uploads get an immediate 400 instead of silently queuing.
- **Rule registry regression fixed**: `CrossBranchCodeRule`/`FormatEStampRule` were imported but never registered, and `VisualStampTripartiteRule`/`VisualStampAmcRule` were actively disabled — all to force the rule count to stay at a stale "47" instead of fixing a real duplicate-registration bug. Restored `FormatEStampRule` and the two visual stamp rules (registry is now **50 rules**). **Deliberately did not restore `CrossBranchCodeRule`** — see "Known gaps" below.
- **Real bug in `date_rules.py`**: the new date-presence rules' PASS branch crashed (`AttributeError`) because it treated a `list[str]` as `FieldValue` objects. Fixed and de-duplicated into `_DateRule._evaluate_presence()`.
- Rewrote `test_date_blank_rules.py` (was testing against a broken hand-rolled `RuleContext` double that was never actually exercised) and several other tests that had hardcoded rule counts or premises contradicting real service-layer rules (e.g. "only one active task per application" is enforced by design, not a bug).
- **Fixed genuine Windows flakiness** in `test_bulk_queue.py::test_multiple_worker_processes_claim_disjoint_jobs`: 3 subprocess workers appended to one shared file, which isn't reliably atomic across OS processes on Windows. Fixed by giving each worker its own file and merging in the test. Verified stable across 15+ repeated runs.

### New this session: Operator Dashboard (frontend)

`/human-review` (nav slot already existed, was an unrouted placeholder) now has a real page: `frontend/src/pages/OperatorDashboard/OperatorDashboardPage.jsx`. Queue table + inline review panel driving the validation task lifecycle (start / complete / reject / request-correction) against Afsana's API, with stored check results and the audit log shown per task. New files: `services/validation.js`, `hooks/useValidationTasks.js` + `useValidationTask.js`, `components/validation/*`, `VALIDATION_TASK_STATUSES` in `data/statuses.js`. Follows existing conventions (CSS Modules, manual `useState`/`useEffect`, no new state library). `npm run build` passes clean. **Browser-verified end-to-end** with Playwright (login as the seeded dev account → create task via API → Start Review → Reject with reason → confirmed status/audit-log updates in the UI at every step).

### 2026-08-13, later same day: second `.enqueue()` bug + a data-loss incident

While helping the user check on a real document they'd uploaded through the running app, found a **second instance** of the same nonexistent-method bug fixed earlier in `upload/services.py` — this one in `document_processing/services.py::_process_bulk_upload()`, which called `QueueJobRepository.enqueue()` (doesn't exist) instead of `enqueue_uploaded_documents(...)`. Effect: bulk uploads split into per-document records successfully, but those documents were never enqueued for OCR/extraction — the pipeline silently stalled forever with no error surfaced anywhere. Fixed in commit `7f3129d`, with a regression test (`test_bulk_upload_split_documents_are_enqueued_for_processing` in `test_bulk_queue.py`) asserting split documents get queue jobs.

**While verifying that fix, running the new regression test wiped the user's real in-progress application** — see the `isolated_database` warning above. The application, its documents, and its queue job were permanently deleted (hard `DELETE`, no soft-delete/backup). The `users` table was unaffected in that instance (temp account still worked), but don't assume that generalizes — treat any pytest run against a database holding real data as destructive to all of `applications`/`documents`/`queue_jobs`/`users`.

**A third, deeper bug in the same area, fixed same day (commit `36bf456`)**: even with the enqueue bug fixed, every bulk-split document was still guaranteed to fail permanently. `process_one()` correctly refuses to OCR/extract a document without a stored PASS technical-validation report (`document_processing/services.py:214`), but the bulk-split path (`_process_bulk_upload`) never ran technical validation on the documents it created before enqueueing them for that exact processing — the queue worker treats a "skipped, not validated" outcome as a job failure and retries it into permanent `FAILED` status (`app/bulk_queue/workers.py:196-199`). Fixed by calling `TechnicalValidationService(self._db).validate(application_id=application_id)` right after `create_many()` and before `enqueue_uploaded_documents()`, mirroring what the manual (non-bulk) upload flow already does via its own separate `POST .../technical-validation/validate` call. A real application (`#2757`) hit this and burned all its retries — unrecoverable, user had to re-upload.

### 2026-08-14: two more bugs found watching a real 21-page bulk upload process end-to-end (commit `a8c41bf`)

With the three bugs above fixed, a real bulk upload (application `#2758`, a 21-page scanned PDF) was watched through the full pipeline live. It split correctly into 3 documents (Tripartite Agreement, Authority Letter, CNIC Front) and passed technical validation, but hit two more real bugs:

- **`ProcessingMethod.OCR` doesn't exist** (`document_processing/services.py`, `_process_bulk_upload`'s success return) — real member is `PADDLE_OCR`. This crashed *after* the split had already fully succeeded, so the split job was wrongly marked failed and scheduled to redo the entire multi-minute OCR pass on retry (which would then hit the `MAX_COPIES` duplicate guard from the bug above and fail again). Fixed.
- **`PROCESSING_TIMEOUT_SECONDS` was `60.0`** (`document_processing/constants.py`) — but PaddleOCR fallback on this CPU-only machine measured **90–150 seconds per page** in this same session. Any multi-page scanned document (e.g. the 2-page Authority Letter) was mathematically guaranteed to time out. Raised to `600.0`. **If OCR is ever run on a machine without a GPU, re-measure per-page time and adjust this constant** — it's a flat per-document budget, not scaled to page count, so a document with many more pages than this session's test case could still need a higher value.

Verified via a static AST sweep (parse every `.py` file under `app/`, cross-reference every `EnumClass.MEMBER` attribute access against the enum's real `__members__`) that no other enum reference in the codebase points at a nonexistent member, and that every repository method called from `document_processing/services.py` actually exists on its class (including inherited `BaseRepository` methods). Worth re-running this sweep after any future change that references enum members by name.

**Known but not fixed, lower priority**: `_process_claimed_job` in `app/bulk_queue/workers.py` treats a `SKIPPED` processing outcome (e.g. "document did not pass technical validation") identically to a real failure — it retries up to `max_attempts` times even though a technical-validation verdict won't change between retries, wasting time before landing on permanent `FAILED`. Also, running two dev processes (uvicorn + the bulk queue worker) against the same `logs/backend.log` causes harmless but noisy `PermissionError` spam on Windows log rotation (`os.rename` can't rotate a file another process has open) — doesn't affect correctness, just clutters the log.

**Unresolved as of end of 2026-08-14 session**: application `#2758`'s bookkeeping is slightly inconsistent — queue job `#4905` (the split job) and `#4907` (Authority Letter's OCR job) were mid-retry when their worker processes were killed to stop a wasteful redo, so they're stuck showing `PROCESSING` in the DB even though no worker is running. The actual documents/data are fine (3 real documents exist, technically validated). A direct SQL fix was attempted but blocked by an automated command-safety classifier that doesn't respond to in-chat approval (only an actual permission-prompt grant, e.g. via Claude Code's Bash permission settings, would unblock it). **Next session: either grant that permission and run the fix, or just start the queue worker again — `recover_stale_processing` will eventually reclaim both jobs on its own (safe, just means Authority Letter's OCR and the (unnecessary) split job both redo one more time before settling).**

### 2026-08-14: two more real bugs found watching a fresh application (`#2759`) through the full pipeline end-to-end

- **The digital/scanned routing threshold was a flat total, not per-page**: a real CamScanner-exported PDF had its watermark ("CamScanner") embedded as real selectable text on every page — 99 characters across 9 pages — which cleared the old flat 10-character bar despite the actual page content being an untranscribed image. The document was routed as "digital," extracted nothing but watermark noise, and every downstream stage (type detection, field extraction, rule validation) failed. Fixed (commit `812010a`): `MIN_DIGITAL_TEXT_CHARS_PER_PAGE = 40`, checked as an average over the real page count.
- **`PROCESSING_TIMEOUT_SECONDS` (flat 600s, raised from 60s earlier in the day) still wasn't enough**: a real 9-page scan needed ~1000s at this machine's measured 90-150s/page PaddleOCR rate and timed out at the 600s mark. A flat total can never work when real uploads range 1-21+ pages. Fixed (commit `a344a2b`): the deadline is now computed *after* routing, once the real page count is known, as `page_count * PROCESSING_TIMEOUT_SECONDS_PER_PAGE` (180s/page, floored at 180s).
- Both fixes verified against the real file/document, end-to-end, through a full manual pipeline run (`technical-validation/validate` → `process-documents` → `analyze-documents` → `confidence/evaluate` → `normalize` → `validate`) — this confirmed the pipeline stages are **not** automatically chained; each is a separate API call a caller (today: only tests, or a human via curl) must trigger in order. No UI currently drives this chain automatically after upload.
- Note: `/process-documents` reprocesses *every* document on the application, including the raw `BULK_UPLOAD` placeholder alongside its already-split children — redundant OCR work (doubles wall-clock time) but not incorrect, since nothing downstream reads the placeholder's OCR result. Not fixed, just documented.

### 2026-08-13 frontend UI session (parallel session, different tool, `feature/zarghuna-bulk-queue`): Validation Report + Human Review + Settings

Completes the operator-facing UI beyond the task queue (scope agreed by user: only these three areas; Dashboard/Application Details/Upload Documents untouched). All API contracts verified against backend source before wiring.

- **Validation Report** at `/reports`: `pages/ValidationReport/ValidationReportPage.jsx` + 8 `components/report/*` sections (summary cards, documents, completeness, technical validation, fields, business rules, visual evidence, issues) fed in parallel by `hooks/useValidationReport.js` from 6 endpoints: `reports/validation-report` (422 → empty state), `completeness`, `technical-validation`, `validation-results`, `analysis-results`, `normalized-fields`. Printable link opens the backend HTML report in a new tab. Apps listed via shared `GET /applications` (default filter `PENDING_REVIEW`). New services: `services/{reports,technicalValidation,analysis,normalization}.js`.
- **Human Review (final review)** at `/human-review`: `pages/HumanReview/HumanReviewPage.jsx` + `components/humanReview/*` (Summary, Documents, Fields w/ corrections, Detections, Checklist, Decision, History) against the **`human_verification` module only** (user decision: deliberately NOT `/confidence/review`). `GET /human-review` → ReviewScreen (15-item checklist default unchecked, confirmed via `_checklist_state`), `POST /human-review` (APPROVE/CORRECT/REJECT; CHECKLIST always sent for every decision; backend enforces approve=full checklist, reject=reason, correct=≥1 correction), `GET /human-review/history`. `hooks/useHumanReview.js` refetches screen+application+history after every submit; `alreadyReviewed` (previous_review or status APPROVED/REJECTED/CORRECTED) renders read-only + ReviewHistory. Reviewer name from `useAuth().user?.name`. 409/validation errors surface as `submitError` on the decision form.
- **Settings** at `/settings`: rewritten `pages/Settings/SettingsPage.jsx` — Profile (name/email/employee_id/role from `useAuth()`/`/auth/me`), Account + functional Sign out (`POST /auth/logout`), Password/Preferences shown as informational "not available" (no backend endpoints — deliberately no fake controls), Administration section kept.
- **Routing/nav**: task queue moved to internal `/validation-tasks` (still routed, not in sidebar); `reports` added to the `PLACEHOLDER_ITEMS` exclusion; nav label "Validation Reports" → "Validation Report". `npm run build` passes clean. Browser-verified in the e2e session below.

### 2026-08-13 theme system session: Light/Mint/Cherry identities + themed scrollbar & logo

Theme layer (`frontend/src/theme/*`, `frontend/src/styles/global.css`) now ships 5 options: Light, Dark, Black, Mint, Cherry. Each explicit theme has its own cohesive token set for page bg, sidebar, navbar, cards, borders, primary buttons + hover, plus theme-aware **scrollbar** (`--scrollbar-track/thumb` via `scrollbar-color` + `::-webkit-scrollbar`, in global.css) and a theme-aware **FinTech brand mark** (`frontend/src/components/common/Logo/Logo.jsx` — inline SVG using `--color-logo-*`, replacing the static `assets/logo.svg` `<img>` in Sidebar/AuthCard/LoadingScreen; shape unchanged). `index.html` pre-render script whitelists all 5 themes. Light primary deepened `#5ab4f0→#1873ba` (white-text contrast 2.28→5.0:1). **System was removed in favor of a monochrome Black theme** (pure-black `#000` surfaces, grayscale accents `#52525b`/`#e4e4e7`, white ≈7.7:1 on primary); **default for fresh visitors is now Light**, and stored `'system'` preferences migrate to Light (`theme.js` `DEFAULT_THEME`, no `matchMedia`/OS-following anywhere). Verified: `npm run build` clean; Playwright theme suite (toggle all 5, per-page sweep, persistence, default/migration) + full UI suite 10/10 green; computed-color/contrast audit passes AA across Light/Dark/Black/Mint/Cherry.

### 2026-08-13 e2e verification session (this session): Playwright, 7/7 green

Playwright suite (scratch, `/tmp/opencode/e2e`, not committed) covers login/sidebar/placeholders, `/reports` (sections, printable HTML popup, FAILED + empty states), `/human-review` (checklist defaults, APPROVE/CORRECT/REJECT flows, read-only already-reviewed + history, empty-app error state), `/validation-tasks`, `/settings` (profile data, sign-out, protected-route redirect). **7/7 pass, stable across 3 consecutive runs**, no console/page/API errors. Browser-verified end-to-end for the first time.

Genuine issues found and fixed during the run:
- **Dev DB was behind alembic head** — `GET /api/v1/validation/tasks` 500'd with `relation "validation_tasks" does not exist`. Fix: `cd backend && .venv/bin/alembic upgrade head` (applied `a3f9b7c1d4e2` validation tables + merge + `02ffa030b9e1` BULK_UPLOAD enum). **Ops note: any environment created before those migrations must run `alembic upgrade head` before the task queue works.**
- **`ReviewFields.jsx` nested `<tr>` smell (previously documented below) is now FIXED**: the correction row is a sibling `<tr>` in a React `Fragment`; the browser now emits zero React DOM warnings.
- **Operator dashboard page title was misleading**: `OperatorDashboardPage` h2 said "Human Review" (duplicating the final-review page at `/human-review`). Changed to "Validation Tasks".

Remaining cosmetic limitation (left as-is): the navbar/breadcrumb label for `/validation-tasks` still falls back to "Dashboard" because it isn't in `data/navigation.js` (it's deliberately internal). Seeded demo data (apps 7504-7510, e2e-* created_by, PDFs in `storage/`) is left in the dev DB for manual browsing; re-seed via `/tmp/opencode/seed_e2e_data.py` (idempotent: wipes `created_by LIKE 'e2e-%'` + storage, restarts id sequence so IDs stay 7504-7510). Login for the seeded UI checks: `EMP-1001` / `employee@fintech.local` / `Welcome@123`.

Note: `ReviewFields.jsx` correction inputs live in a sibling `<tr>` (see above) — any selector for the corrected-value input must target the row following the field row, not a child of it.

### 2026-08-13 enterprise Settings + functional workspace preferences (committed `1213cf4`, pushed on `feature/zarghuna-bulk-queue`)

Redesigned `/settings` into an enterprise workspace and made all preferences genuinely functional. No backend/API/routing changes. **All 7 preferences are stored under one localStorage key `fintech-workspace-preferences`** via `frontend/src/utils/preferences.js` (`getPreferences/getPreference/setPreference`; defaults: `rememberApplicationFilter: true`, `rememberLastOpenedApplication: true`, `confirmBeforeDeleteDocument: true`, `confirmBeforeRejectApplication: true`, `autoStartProcessingAfterUpload: false`, `autoRefreshProcessingStatus: true`, `openReportOnProcessingComplete: false`).

- **Settings page** (`pages/Settings/SettingsPage.jsx` + `.module.css`, token-only styling, adapts to all 5 themes): **Profile** (avatar initials, name, role, copy-to-clipboard buttons for Email + Employee ID with `navigator.clipboard` + execCommand fallback + toast, read-only — no fake editing), **Workspace Preferences** (7 `PreferenceRow` toggles — new `components/settings/PreferenceRow/*` + `components/common/Toggle/*` — with 1.5s "Saved" feedback and a "Changes are stored on this device." note), **Account & Session** (signed-in-as, role, "Session active" chip — never implies a backend account-status field, which doesn't exist — and Sign out via `POST /auth/logout` behind a ConfirmDialog + spinner), **Password & Security** (honest informational panel, no fake form), **Administration** (Feedback + Continuous Learning kept, "Restricted" badge). No theme selector, no notification section (per scope).
- **Every pref is wired into the app**, not decorative: filter persistence in `useApplications` (initialized from + writes back `applicationFilter`); "Resume Application #N" chip on the Applications toolbar (`hooks/useLastOpenedApplication.js` + `ApplicationsPage`, gated on `rememberLastOpenedApplication`, recorded by `ApplicationDetailsPage`); `confirmBeforeDeleteDocument` bypasses the ConfirmDialog in `UploadDocumentsPage`; `confirmBeforeRejectApplication` routes the REJECT decision through a ConfirmDialog in `HumanReviewPage`; `autoStartProcessingAfterUpload` fires `startProcessing(applicationId)` (idempotent) only after a *successful* upload/bulk result; `autoRefreshProcessingStatus` gates the 2.5s polling in `useProcessingProgress` (when off, a manual **Refresh** button was added to `ProcessingProgress`); `openReportOnProcessingComplete` navigates exactly once (ref-guarded) to `/reports?application=<id>`, which `useValidationReport` reads via `useSearchParams`.
- **Genuine bug found & fixed**: `useHumanReview.loadApplications` was last-writer-wins with no staleness guard — an overlapping list fetch (React StrictMode double-effects in dev, or rapid filter switches) could resolve out of order and wipe the app dropdown to `[]` (observed intermittently as a Playwright timeout on the reject flow). Fixed with a request-id ref so stale responses are ignored. Real user-visible bug, not just test flakiness.
- Verification: `npm run build` clean; scratch Playwright suite (now **15 tests**: theme ×3, settings-prefs ×5, ui ×7) **15/15 green across repeated runs** — prefs persist + "Saved" feedback, remembered filter restored after refresh, resume shortcut, polling stops when auto-refresh off, report opens exactly once (mocked progress API), reject confirmation dialog, all 5 themes sweep every page, sign-out → `/login`. Toggle gotcha: Playwright `getByRole('switch')` matches 0 for `opacity:0` inputs — tests target `input[type="checkbox"]` + `setChecked()`; real pointer clicks verified via a probe (thumb had to get `pointer-events: none`).

### 2026-08-13 application name feature + dummy-data cleanup (uncommitted on `feature/zarghuna-bulk-queue`)

Two things, per user request:

- **Removed the seeded e2e dummy data from the dev DB** (apps 7504-7510, `created_by LIKE 'e2e-%'`) and their `storage/` trees, and reset `applications_id_seq` so the next id is 7512. **Do NOT re-run `/tmp/opencode/seed_e2e_data.py`** — the user wants those dummy apps gone. (During cleanup the user's own real app #7511 appeared and was left alone; it later disappeared from the DB on its own during this session — possibly the user's own cleanup or the bulk worker consuming it. Dev DB now has **zero applications**; login account restored.)
- **Application name feature**: applications now have a display name derived from the uploaded PDF filename. `Application.name` nullable `String(255)` column + migration `backend/alembic/versions/7a1b2c3d4e5f_add_application_name.py` (down_revision `02ffa030b9e1`, applied). `UploadService.upload` (first uploaded document only) and `upload_bulk` set `application.name` when NULL via new `_display_name_from()` helper, which strips the extension using `Path(filename).stem` (user confirmed: strip `.pdf`). Later uploads never overwrite an existing name. `ApplicationBase` schema exposes nullable `name` (max_length 255).
- **Frontend**: Name column in the Applications table (`ApplicationTable.jsx` header + `ApplicationRow.jsx` Name cell, falls back to muted `—` when NULL); `useApplications` search matches `application.name`; Human Review + Validation Report app dropdowns show `#{id} — {name || created_by} ({status})`; Application Details page shows a Name field; dashboard `RecentApplications` shows the name above created_by.
- **Verification**: full backend suite **709 passed** (was 704; +5 new name tests in `test_upload_api.py`/`test_bulk_upload_api.py`). NOTE: the full suite now takes **~10 min** on this box — a 10-minute timeout will look like a hang and mid-run it wipes all users (breaking login) until session teardown re-seeds; restore with `cd backend && .venv/bin/python -m app.auth.seed`. `npm run build` clean; live end-to-end verified through the vite proxy (create app → bulk-upload `TMA Khal Dir Lower.pdf` → name `TMA Khal Dir Lower` in list; test app cleaned up afterward).

### 2026-08-13 Processing page replaces "Upload Documents" sidebar entry (uncommitted)

Per user request, the sidebar "Upload Documents" item (a dead-end redirect to `/applications`) is replaced with a real **Processing** page at `/processing` (`pages/Processing/ProcessingPage.jsx` + `.module.css`, `hooks/useProcessingOverview.js`). It shows every application's processing status: summary cards (apps, processing now, documents left, need attention) and per-app rows with a progress bar (`completed of total done · N left`), queued/processing/failed/completed chips, a Retry button for failed documents, and a link to the application. Reuses existing endpoints (`GET /applications` + `GET /applications/{id}/processing/progress` per app, `POST .../processing/retry`). Auto-refreshes every 2.5s while any job is queued/in-flight, gated by the `autoRefreshProcessingStatus` preference; manual Refresh button otherwise. The old `/upload` route and its redirect, and the `/processing` placeholder entry in `INTERNAL_ROUTES`, were removed; `PLACEHOLDER_ITEMS` now excludes `'processing'`. Sidebar label: **Processing** (Activity icon). Verified live in Playwright (login EMP-1001 → `/processing` renders real queued app with `0 of 1 done · 1 left`; summary counts correct; no page errors). Note: the 401 console messages seen on cold boot are the normal `/auth/me` + `/auth/refresh` session-boot cycle (StrictMode double-fetch), not this page.

### 2026-08-15: `Application.status` was dead for the whole middle of the lifecycle (foundation audit, Section 1-2 findings)

A full-foundation audit (architecture, data layer, security, etc. — read-only, section by section) found that `ApplicationStatus.PROCESSING` and `ApplicationStatus.PENDING_REVIEW` were **never set anywhere** — grepped every write site: the only place `Application.status` was ever assigned was `human_verification/services.py`'s final-decision submission. Every application showed "Pending" (the `SUBMITTED` label) from creation through OCR, analysis, confidence, normalization and rule validation, and only jumped straight to Approved/Rejected/Corrected once a human decided — `useHumanReview.js`'s status filter, which defaults to `PENDING_REVIEW` (correct design, just aimed at a status nothing produced), silently showed zero applications the entire time.

Fixed: guarded `SUBMITTED → PROCESSING` writes at the three real enqueue call sites (`upload/services.py::upload_bulk()`, `bulk_queue/services.py::enqueue_application()`/`start_processing()`), a `PROCESSING → PENDING_REVIEW` write at the end of a successful `PipelineRunnerService.run()`, and a new terminal status, `PROCESSING_FAILED` (migration `e1f2a3b4c5d6`, chained on `c8d9e0f1a2b3`), written when `_maybe_start_pipeline`'s `PIPELINE_BLOCKED` branch fires (every document failed, so the pipeline never runs — previously this left the application silently stuck at `PROCESSING` forever with no visible sign anything had gone wrong). All writes are guarded to only fire from the expected prior status, so a retried job or a document added after a decision can't regress a status a human has already set. `useHumanReview.js`'s default needed no functional change (it was already correct, just aimed at a status that didn't exist yet) — added a comment explaining the dependency instead. Frontend: `statuses.js` + a new `rose` badge color for `PROCESSING_FAILED`, distinct from `red` (Rejected) and `orange` (Under Review).

Known, accepted limitation carried into this fix, not introduced by it: the guarded writes in `bulk_queue/services.py` read `application` once at the top of the method, before real work (technical validation, job creation) runs, then act on that in-memory value later — the same unprotected read-then-write shape already flagged below for `human_review`/`validation_task`/`validation_run`, not a new category of risk.

Also removed `PROJECT_FULL_CONTEXT.md` (an untracked, 903-line static architecture/API/schema dump that had been sitting in the repo root, never committed). It duplicated ground `README.md` already owns (stack, architecture, DB schema, setup, API surface) and, on comparison, contained claims that were stale or aspirational rather than verified against running code — notably describing the `SUBMITTED → PROCESSING → PENDING_REVIEW → ...` lifecycle above as already-implemented before today's fix actually built it, and describing the `validation` module (see the new known-gap entry directly below) as a normal, integrated pipeline step rather than the disconnected, untriggered module it actually is. Nothing in it was decision-relevant that this file doesn't already cover in more detail and with real dates/evidence.

**`validation` module (ValidationTask/ValidationRun/ValidationLog, Operator Dashboard at `/validation-tasks`) is vestigial, not awaiting integration** — confirmed exhaustively during the same audit: `grep -rl "from app.validation.services import" app` matches only the module's own `routes.py`/`__init__.py`; no scheduler/cron exists anywhere in this codebase; neither benchmark script under `backend/scripts/` references it; the frontend's `createValidationTask` (`services/validation.js`) has zero callers anywhere in the UI. Nothing, anywhere, ever creates a `ValidationTask` in real usage. Three reasons this reads as superseded rather than incomplete: (1) both `ValidationTask` and `ValidationLog`'s own docstrings self-describe as scaffolding for integration that never arrived (*"the unit of work the validation queue hands to an operator **in a later phase**"*, *"the separate User/Operator module will attach the acting operator through **a future migration**"*); (2) `human_verification` already independently and completely implements the same real-world job — an operator reviewing an application's fields, checklist, documents and visual findings, then recording approve/correct/reject — without ever touching `validation`, and it's the module the auto-pipeline (`c3179e1`) actually leads into; (3) the routing history already documented below shows `/human-review` was deliberately repurposed away from this module's Operator Dashboard toward the final-decision screen, demoting the task queue to an un-navigable URL, with no nav entry ever restored. Reconciling the two overlapping designs is a product decision (does a two-stage triage-then-decide workflow across two screens actually get built, or is the one-shot screen the real design), not a code fix — do not wire this module up reactively. Banner comments pointing here were added to `validation/routes.py` and `OperatorDashboardPage.jsx`.

**[ADDENDUM 2026-08-22, "validation module fate" investigation] Not "two designs to reconcile" — one already-built real workflow, plus one abandoned prototype a later feature superseded without ever deleting it.** Re-investigated from scratch (STEP 1 re-verification, not trusted from the entry above): the frontend's `/validation` sidebar entry is navigable today and does render a real, live, OPERATOR-gated page — but it's `pages/Validation/ValidationPage.jsx` calling `app/operator_workflow` (`GET /validation/applications` queue, `request-documents`/`operator-reject`/`operator-submit`), a **separate module that didn't exist at the time of the finding above** and was built later to give operators a genuine business-completeness triage screen, fully independent of `app.validation` (confirmed: zero imports of `app.validation.services` anywhere in `app/operator_workflow`). `OperatorDashboardPage.jsx` (the component the banner comment above references) and `services/validation.js`/`createValidationTask` are **already gone from the tree entirely** — not just unlinked, genuinely deleted at some point before today, with zero remaining matches anywhere in `frontend/src`. So the "un-navigable URL, no nav entry ever restored" framing above is stale: the URL is navigable, just by a different, newer module than the one that finding was describing. The core conclusion stands and is reconfirmed independently today: `app.validation` (`ValidationTask`/`ValidationRun`/`ValidationLog`) still has zero real callers anywhere. **Decision (Abdul, 2026-08-22): delete `app.validation` outright** rather than build out its original two-stage design — `operator_workflow` (triage) + `human_verification` (final decision) already are that two-stage workflow, just under different module names than originally scaffolded. See this file's later entries for the actual removal.

**Master Rules §3's AMC↔Tripartite/Bilateral branch-name consistency check has no implementing rule at all (found 2026-08-16, reconciling the AMC extractor merge)** — `docs/Master_Rules_Combined.md` §3 requires the Account Maintenance Certificate's Account Title, Account Number, *and* Branch Name/Number to match the Tripartite and Bilateral agreements. `AccountMaintenanceCertificateExtractor` and `TripartiteAgreementExtractor` (`document_analysis/extractors.py`) extract account title/number under the matching field name (`account_holder`/`account_number`), so `CrossAccountHolderRule`/`CrossAccountNumberRule` (`rule_engine/rules/cross_document_rules.py`) do cover 2 of the 3 required checks. The branch portion is not wired at all: AMC's extractor names its field `branch_name`, while the one rule that compares branch info (`CrossBranchCodeRule`) reads `branch_code` and lists `ONE_LINK_LETTER`/`TRIPARTITE_AGREEMENT`/`AUTHORITY_LETTER` as participants — `ACCOUNT_MAINTENANCE_CERTIFICATE` isn't a participant at all, and the field name wouldn't match if it were. This is independent of the pre-existing reason `CrossBranchCodeRule` itself stays unregistered (below): even once that rule is safe to register, it still wouldn't implement §3's actual requirement, because it was never scoped to include AMC. Not attributed as anyone's error — just undocumented until now. Closing this needs either a new/extended cross-document rule scoped to AMC+Tripartite+Bilateral on branch info, or renaming/aliasing one of the existing fields, plus real branch extraction on whichever documents are made participants.

## Known gaps (verified 2026-08-13, don't re-discover from scratch)

**`fcc9fda`'s blank-page technical-validation check fails against the test suite's own shared image fixture, causing 13 currently-failing tests in `test_document_analysis_api.py` (found 2026-08-16/17, verifying the AMC/BRD merge)** — `TechnicalValidationService`'s new `is_blank_image()` check (`technical_validation/utils.py`, `BLANK_INK_RATIO = 0.005`) flags the shared image fixture used across `test_document_analysis_api.py` as blank (`TECH_BLANK_PAGE` FAIL, "Page contains no readable content"). That fails technical validation, which makes `document_processing` skip the document ("did not pass technical validation"), which leaves no `OCRResult` row, which makes `document_analysis` fail with "No OCR result found for the document; run document processing first" — the document never reaches `ANALYZED`, cascading into 13 test failures (`assert 'FAILED' == 'ANALYZED'` and downstream assertions in the same file that depend on it). **Confirmed pre-existing and unrelated to this session's Phase 1 (BRD) or Track B (AMC/Tripartite) work**: all 13 independently reproduced, by name, in an isolated `git worktree` at `origin/main` (`5225089`, before this session's rebase touched anything) — same 13 test names, same failure signatures, run individually not just grouped by assumption. It is live on `origin/main` right now, not merely a local artifact of this session's branch reconciliation. **Needs to be raised directly with Zarghuna** (author of `fcc9fda`) since it's her threshold/fixture interaction and she can explain or fix it fastest — not something this session's Phase 1 scope should touch.

**Rule engine coverage vs. `docs/Master_Rules_Combined.md`** — no automated coverage for: blank-date enforcement on Tripartite/Bilateral preambles, PayMin/Digital Muhasil/Paymere BCX terminology matching, organization-name consistency across documents, per-page 1-Link signature requirement, document layout/point-numbering conformance, E-Stamp visual authentication, Notary Public stamp detection, Formal Request Letter subject-line check, CNIC expiry/completeness. (Schedule-of-Charges/BRD signature+stamp checks are deliberately handled via the human-verification manual checklist instead.) **None of these fields have any extraction/normalization support in the codebase today** — confirmed by grep. Do not add a rule for any of them without adding real extraction first: see the `CrossBranchCodeRule` incident above for exactly what goes wrong otherwise (`_CrossDocumentRule` hard-`FAIL`s on a missing field; `_FormatRule` only WARNs; `_VisualRule` degrades to `PENDING_MANUAL_REVIEW` — know which base class before wiring a new rule in). **Teammate-reported signature/stamp requirements by type, added 2026-08-17 for reference only — not independently verified against real samples, no extraction or rule added.** Teammate's list: sign needed — Authority Letter, Account Maintenance Certificate, 1-Link Letter, Tripartite Agreement, Schedule of Charges, Business Requirement Document, and an unresolved "Onboarding" item (see below); stamp needed — Account Maintenance Certificate, 1-Link Letter, Tripartite Agreement, Schedule of Charges. Cross-checked against the master rules doc's per-type "Signature & Stamp Requirements" sections: excluding the unresolved "Onboarding" item, the sign-needed list's other 6 items are all resolvable, and all 6 match an existing section (§2 Authority Letter, §3 AMC, §4 1-Link Application Form, §6 Tripartite, §9 Schedule of Charges, §10 BRD). **Two discrepancies found, not resolved**: §2 and §10 both state a conditional stamp requirement ("where prescribed" / "where required by its approved format") for Authority Letter and BRD respectively, but the teammate's list marks neither as needing a stamp. **Two omissions**: Bilateral Agreement (§7, explicit sign+stamp requirement) and Formal Request Letter (§11, sign+stamp "where prescribed") appear in neither of the teammate's two lists at all, despite the master doc covering both. **"Onboarding" is unresolved, not guessed at**: grepped both this file and the master rules doc for every use of the word — none names a distinct checklist/document type; the only matches are generic prose describing the document set's purpose, "the onboarding department" as a description of the sub-biller party (§6), a suggested Formal Request Letter subject-line phrase (§11), and the unrelated "X Onboarding Documents.pdf" source-file naming convention (see the real-file list above) — needs to go back to whoever supplied the list for clarification, not assumed to mean any of the 8 required types or CNIC. One more nuance worth flagging: 1-Link Letter's real content (per the checklist-mismatch entry above) is actually a Participation Memorandum, which §8's more specific E-Stamp/Notary-Public requirements (visual stamp-paper authentication, notary seal) plausibly govern instead of §4's simpler sign+stamp pair — which section's signature/stamp rule genuinely applies to it is entangled with that still-open mismatch, not something this cross-check resolves.

**Two independent `CRITICAL_FIELDS` lists** (`document_analysis/constants.py`, per-document-type dict, vs `confidence/constants.py`, flat global set) are deliberately different shapes for different purposes — not a bug, now documented in-code.

**Dead-looking but intentionally-kept code**: `DatasetValidationError`, `InvalidReportRequest` — defined, exported, documented in `docs/` as part of their module's exception contract, never raised. Left alone rather than deleted.

**Bulk-splitter and document-analysis use two independent, non-overlapping classifiers (found 2026-08-14, Phase 4 pipeline-orchestration work)** — `app/preprocessing/splitter.py`'s page classifier recognizes the real required-document vocabulary (literal phrases like "TRIPARTITE AGREEMENT", "ACCOUNT MAINTENANCE CERTIFICATE"). `app/document_analysis/extractors.py::detect_document_type`'s classifier recognizes only 4 unrelated categories via its own separate keyword table: bank statement, payslip, ID document, tax document. A document can be split and typed correctly by the first classifier and still fail analysis with "Document type could not be determined" from the second, because the two share no vocabulary. Confirmed directly: a bulk-PDF page containing "TRIPARTITE AGREEMENT" splits cleanly into a `TRIPARTITE_AGREEMENT`-typed document but is permanently unanalyzable — `document_analysis_results` never gets a row for it (see `tests/test_pipeline_orchestration.py`, which had to switch to bank-statement-shaped fixture text to get a real document through analysis at all). **Impact**: any bulk-uploaded application containing non-financial required document types (agreements, letters, certificates — most of the actual checklist) has those documents silently excluded from field extraction and confidence scoring; only a per-document log line (`Document type could not be determined from the extracted text`) records it. Not fixed as part of Phase 4 — pipeline orchestration only chains the four existing post-OCR calls, it doesn't touch either classifier. Needs its own scoping pass: either widen `detect_document_type`'s vocabulary to cover the real required-document checklist, or make `document_analysis` explicitly tolerant of document types it has no extractor for (right now `UnsupportedDocumentType` is a hard per-document failure).

**`validation` module has no actor/reviewer field anywhere (found 2026-08-14, reviewer-identity-from-session work)** — `ValidationTask` and `ValidationLog` (`database/models/validation_task.py`, `database/models/validation_log.py`) record *what* happened (status transitions, field verify/correct/complete/reject actions) but never *who* did it: no `performed_by`/`actor`/`username` column on either model, and none of the module's request schemas (`CompleteRequest`, `RejectRequest`, `RequestCorrectionRequest`, `FieldVerifyRequest`, `FieldCorrectRequest`, `EvidenceReviewRequest` in `validation/schemas.py`) carry one either — confirmed by reading every field on both models and every schema. This is a different shape of gap than the `reviewer_name`/`created_by` issue fixed elsewhere in this same session (`human_verification`, `confidence`, `upload`): those fields *existed* but were spoofable from the request body; here the field doesn't exist at all, so there's no session-swap fix available — closing this needs an actual migration (new column on both tables) plus wiring `current_user` through the module's routes/services the same way, plus backfill/nullability decisions for existing rows. Scoped as its own follow-up, not bundled into the reviewer-identity commit.

**[RESOLVED 2026-08-22] Two unused/dead code paths found via coverage data (2026-08-15, foundation audit Section 5)**: `ImageEnhancer` (deskew/CLAHE/denoise, `app/preprocessing/enhancer.py`) is present but unused — `grep -rn "enhancer" app/` matches nothing outside the file itself; pending a product decision on whether image enhancement belongs in the preprocessing pipeline. `ValidationLogRepository.bulk_create()` (`app/database/repositories/validation_log_repository.py:85-101`) has zero callers anywhere in `app/` — same dead-code shape, not a coverage bug to fix.

**UPDATE 2026-08-22**: `ImageEnhancer` half resolved — the "pending a product decision" framing was stale; the decision had already been made and documented, just not executed. `app/preprocessing/__init__.py`'s own docstring already stated "Image enhancement intentionally lives in `app.document_processing.utils` (`deskew_image`, CLAHE, denoise) and is not duplicated here" — confirming the real, equivalent implementation (`document_processing/utils.py::preprocess_image`) was already the intended one, and `enhancer.py` was leftover. Re-confirmed zero callers/zero tests by fresh grep, then deleted outright (no `__init__.py` edit needed — it never imported the file). `ValidationLogRepository.bulk_create()` is unrelated and still unresolved, not touched by this change.

**Splitter's OCR-classification fallback skips the real preprocessing pipeline (found 2026-08-22, during the `ImageEnhancer` investigation — unrelated to that decision, just surfaced by reading the same code)** — `app/preprocessing/splitter.py`'s `_classify_page` (lines ~415-437) falls back to a direct PaddleOCR call when a page's native PDF text is too sparse to classify: it renders the page to a raw pixmap, converts RGBA→RGB if needed, and calls `ocr_engine.extract(img)` directly. It does **not** call `document_processing/utils.py::preprocess_image` (grayscale → deskew → denoise → CLAHE → adaptive threshold) first, unlike every real extraction path (`document_processing/processors.py` lines 329 and 364, which always preprocess before OCR). So a skewed or noisy scanned page could get a worse classification-fallback read than the same page gets later during real extraction — possibly affecting which document type it's classified as during splitting. Not fixed, not sized, no real sample confirmed to be affected yet (would need a real skewed/noisy page that also has sparse native text to trigger this path and show a difference) — just recorded so it isn't lost. If picked up, the fix is calling the existing `preprocess_image` in that fallback branch, not `ImageEnhancer` (deleted, and not the right tool even if it weren't).

**[PARTIALLY RESOLVED 2026-08-22] Health check only verifies DB connectivity — does not monitor the queue worker process or storage. Not a production-ready health check** (found 2026-08-15, foundation audit Section 6). **UPDATE 2026-08-22**: `app/api/health.py` now also checks the dedicated queue worker's heartbeat file (`_worker_heartbeat_message`, `_HEARTBEAT_STALE_AFTER_SECONDS = 60`) — a stale or missing heartbeat reports HTTP 200 with `degraded: true` and a `details.worker` message, distinct from a DB failure (which reports top-level `status: degraded`). Worker liveness is fixed; **storage is still not monitored at all** — confirmed by reading the full file, no storage/disk check exists anywhere in it. Not fully resolved, only the worker half.

**[RESOLVED 2026-08-22] `AuditLog` (`app/database/models/audit_log.py`) is written from 5 real call sites but never read back anywhere — no API route, no UI (found 2026-08-15, foundation audit Section 6)** — checked every `routes.py` in the backend and every frontend service file, and `README.md`/`backend/docs/*.md` for a documented decision: the docs describe what gets written (which action strings, by which module, at which log level) but nowhere state a decision to keep it inaccessible. No record of this being deliberate — it's a real missing feature (or a pending product decision on whether it should ever be surfaced), not an intentional design choice. Don't confuse with `ValidationLog`, a different model the vestigial Operator Dashboard does display.

**UPDATE 2026-08-22, re-investigated for the actual viewer decision: the original finding's premise was wrong on two counts, re-verified directly against code, not carried forward from memory.** (1) **"Never read back anywhere" was false** — `app/system_logs/` (a module that either didn't exist yet or was overlooked on 2026-08-15) already implements a complete, tested, IT-role-gated read API: `AuditLogRepository.search()` (filter by application/actor/event_type/severity/date range/free text, paginated) is called by `SystemLogService`, exposed as `GET /system-logs` + `GET /system-logs/{id}` (`app/system_logs/routes.py`, registered in `app/api/__init__.py`'s `protected_router`, `require_role(ROLE_IT)`-gated), and covered by 9 passing tests in `tests/test_system_logs_api.py`. The only real gap was the frontend — zero references to `system_logs`/`system-logs`/`audit` anywhere in `frontend/src` before today. (2) **"5 real call sites" was also stale** — re-counted directly by grep: 17 write call sites across 7 modules (`confidence/services.py` ×6, `human_verification/services.py` ×3, `operator_workflow/services.py` ×3, `bulk_queue/workers.py` ×2, `normalization/services.py` ×1, `rule_engine/services.py` ×1, `upload/services.py` ×1), not 5 — the write side had grown since the original count and nobody had re-verified it. **Decision (Abdul, 2026-08-22): build the frontend viewer** rather than leave it write-only or fold it into `application_history` (rejected: `application_history`'s timeline is sourced from `ValidationHistoryEntry`/`Document`/`QueueJob`/`HumanReview`, not `AuditLog`, so merging would mean de-duplicating two independently-designed, partially-overlapping event sources for no real gain over `system_logs`'s own clean, already-complete backend). Shipped same day, following the `application_history`/`ApplicationHistoryPage` list-plus-detail-panel pattern exactly: `services/systemLogs.js`, `hooks/useSystemLogs.js`, `pages/SystemLogs/SystemLogsPage.jsx`, "System Logs" nav entry (`strictRoles: ['IT']`), route registration, 9 new unit tests (`frontend/src/__tests__/pages/SystemLogsPage.test.jsx`) plus updated IT-role-gate coverage in `Phase1Routes.test.jsx`/`roles.test.js` (`9c33446`, pushed). Frontend suite 108/108 passed (up from 97/17 files to 108/18), 0 lint errors, build clean. Closed.

**[RESOLVED 2026-08-22] PaddleOCREngine singleton is shared across all worker threads with no locking — RISKY (found 2026-08-15, foundation audit Section 10)**: concurrent `.predict()` calls from multiple workers can trigger a known PaddlePaddle crash bug (PaddlePaddle/PaddleOCR#17787). The singleton exists and is shared (tested by `test_paddleocr_engine_singleton_is_reused`), but no test exercises concurrent calls. This is a correctness/stability risk under normal concurrent operation (2 workers default), not a throughput ceiling. Mitigation: either add a per-process lock around `.predict()`, or ensure only one worker thread calls it at a time (e.g., a dedicated OCR subprocess). **UPDATE 2026-08-22**: `app/document_processing/processors.py::PaddleOCREngine._predict_lock` (a class-level `threading.Lock`, shared across every instance since they all call into the one underlying predictor) now serializes every `.predict()` call — confirmed present and wired into `extract()`. Also worth flagging while re-verifying this: `bulk_queue_workers` actually defaults to **1**, not 2 (`app/core/config.py:161`) — the class's own docstring still says "2 by default," which is itself stale, but doesn't affect whether this gap is closed, since the lock protects correctness regardless of worker count.

**Modal focus management missing (found 2026-08-15, foundation audit Section 11)** — `SessionTimeoutModal` and `ConfirmDialog` both use correct ARIA roles/attributes (`alertdialog`, `aria-modal`) but neither moves focus into the dialog nor traps Tab within it. A keyboard/screen-reader user can Tab through background content behind a visually-obscured dialog. Present in both modal components; affects every modal interaction in the app.

**[RESOLVED 2026-08-22] Zero automated frontend tests (found 2026-08-15, foundation audit Section 11)** — `package.json` has no Jest, Vitest, Playwright, or Testing Library. No `.test.`/`.spec.` files anywhere in `src/`. All prior Playwright verifications (theme suite, settings-prefs, Operator Dashboard, Processing page) were ephemeral, run from `/tmp` and never committed — this file confirms that explicitly elsewhere in its own history. ESLint catches static issues but nothing runs the app behaviorally. This means bugs found and fixed in those sessions (`useHumanReview.loadApplications` race, `Toggle` `aria-hidden`) have zero regression protection. **UPDATE 2026-08-22**: no longer true — `package.json` has `vitest`/`@testing-library/react`/`@testing-library/jest-dom` (`npm run test` watch, `npm run test:ci` single-pass), and 17 `.test.jsx` files exist across `src/` today (confirmed by direct count, not by memory of an earlier session). Last full-suite count on record: 97 passed across those 17 files (2026-08-20 entry above, Application History work) — not re-run as part of this correction, so treat that count as the last-known, not freshly re-verified today.

**[RESOLVED 2026-08-22] ENVIRONMENT single point of failure (found 2026-08-15, foundation audit Section 7)** — `ENVIRONMENT` defaults to `"development"` (`app/core/config.py:100`). All three production safety mechanisms (DEBUG guard, SECRET_KEY guard, seed-script password guard) depend on it being explicitly set to `"production"` — nothing enforces this or checks it. A deployer who forgets to set `ENVIRONMENT` will silently deploy with `DEBUG=True`, the dev `SECRET_KEY`, and the dev seed password. This was documented nowhere in the repo before this entry — only in the audit transcript that found it. **UPDATE 2026-08-22**: fixed — `environment: Environment = Field(default="production")` (`app/core/config.py:117`), fail-closed by design per its own docstring ("an omitted `ENVIRONMENT` must never silently enable development behaviour"). An omitted value now defaults safe; local dev/CI set it explicitly instead (`.env.example` ships `ENVIRONMENT=development`, CI uses `testing`). Confirmed directly in code, not from memory.

**[RESOLVED] Missing worker-process documentation (found 2026-08-15, foundation audit Section 12)** — `backend/docs/phase15e-reliability.md` documents the dedicated worker process (`python -m app.bulk_queue`) and `bulk_queue_background_drain=False` for production, but README.md's "Running the application" section and "Module documentation" table do not reference this file. A new developer following README's documented path would never discover the worker is a separate process that must be run. **UPDATE 2026-08-22**: no longer true, found already fixed while auditing this file for stale entries (no code/docs change made today, no commit attributed) -- `README.md`'s "Running the application" section (line ~330) states plainly that local dev "requires **two backend processes**, not one" and links `phase15e-reliability.md` for the full worker execution model; its "Module documentation" table also lists `phase15e-reliability.md` ("Phase 15E — bulk queue worker execution model"). Both references this entry said were missing are present.

**2026-08-16: Phase 0 (splitter fix, `b040927`) and Phase 1 start — Bilateral Agreement extractor (`08bec31`) — both committed to `main`, not yet pushed.** Phase 0's watermark/OCR-fallback threshold fix (see the CamScanner entries above) is verified end-to-end on one real file (`TMA Khal Dir Lower .pdf` — splits into multiple distinct types instead of collapsing into one blob) plus the full regression suite (746 passed). Phase 1 added the first real checklist-type field extractor (`BilateralAgreementExtractor`, `document_analysis/extractors.py`), fixed a routing-precedence bug (the splitter's classification must be checked before the generic keyword-based `detect_document_type`, not after — otherwise a real Bilateral Agreement's own "Account Number"/"IBAN" fields misroute it as a fake bank statement), and unregistered `CrossPeriodRule` (rule registry now 49, was 50) after confirming it would otherwise permanently block every real application from reaching `APPROVED` — see the two-classifier gap entry above, which Phase 1 is now actively closing one document type at a time rather than all at once.

**21-real-file splitting verification was started for extra confidence beyond the one file already proven, then deliberately stopped 2026-08-16 at 2/20 files complete to reclaim CPU for Phase 1 work ahead of a demo deadline — this is honestly partial, not completed, coverage.** Files 1-2 (`Conservator Wildlife Peshawar Zoo.pdf`, `DG Sports KP Onboarding Documents.pdf`) both split correctly into 7 documents each, confirming the Phase 0 fix generalizes beyond the original test file. The other 18 files were never run. Phase 0's fix does not depend on this job finishing — it was already independently proven via one real file plus the full regression suite before this verification job was even started. Also notable: neither of these first two real files produced a `BILATERAL_AGREEMENT`, `TRIPARTITE_AGREEMENT`, `ONE_LINK_LETTER`, `SCHEDULE_OF_CHARGES`, or `FORMAL_REQUEST_LETTER` classification at all — every page in both files matched one of only 4 types (`ACCOUNT_MAINTENANCE_CERTIFICATE`, `AUTHORITY_LETTER`, `CNIC_FRONT`, `BUSINESS_REQUIREMENT_DOCUMENT`). Unclear yet whether that's because those two specific organizations' packets genuinely lack those document types, or whether the splitter's title-phrase matching has uncovered variants for them the way it did for Authority Letter (see below) — worth checking against a file that's supposed to have those types before assuming the splitter is fully correct on them.

**2026-08-16, same day: checklist/manifest cover-page false-positive found and fixed (`68d47d6`).** Investigating why the 21-file job's `DG Sports KP Onboarding Documents.pdf` run (above) showed `AUTHORITY_LETTER: 2` — via the new OCR cache, not a raw re-OCR — found the "second copy" is actually a checklist/manifest cover page whose own first table row literally reads "Authority Letter of officer – Signed" (listing every required document by name); that row fell inside the splitter's header zone and strong-matched as if it were the document's own title. The real Authority Letter is a separate, genuine single document later in the file. Fixed generically in `preprocessing/splitter.py`: any page whose header zone starts with "CHECKLIST" is now excluded from all title matching (both the strong per-line match and the weak substring fallback), not just for Authority Letter — the same false-positive risk exists for any of the 9 checklist type names appearing in such a table. `upload/constants.py::MAX_COPIES_BY_DOCUMENT_TYPE` did not need changing; this was never a real multi-copy case. Regression test added and verified (via `git stash`) to genuinely fail without the fix. Full suite: 747 passed. This also plausibly explains the paragraph above's open question about why `TRIPARTITE_AGREEMENT`/`BILATERAL_AGREEMENT`/etc. never appeared in the first 2 files' classification — worth re-checking once there's time, not confirmed yet.

**New reusable dev tool: `backend/scripts/ocr_cache.py`** — OCR is the dominant cost of any real-file work on this CPU-only machine (minutes/page); this caches each split document's OCR'd text on disk at `Confidential Data/.ocr_cache/` (already covered by the existing `/Confidential Data/` gitignore rule, verified with `git check-ignore`), keyed by source file + document type + copy number, so a given (file, type, copy) is only ever OCR'd once. `get_ocr_text(source_file, document_type, copy_number)` is the one path any future real-sample validation should go through instead of an ad-hoc one-off script that throws the OCR'd text away afterward. **Correction found 2026-08-16 late session**: caching is incremental, not all-or-nothing — `_split_and_cache_all` writes each split document's cache file as soon as that document's OCR finishes, not only when the whole source file is done. A large scanned file (e.g. `GDA Abbotabad.pdf`, 41 pages) can run for 90+ minutes, but partial results are readable from `Confidential Data/.ocr_cache/` well before the job completes — `ls`/`grep` the cache directory instead of assuming a still-running job has produced nothing yet.

**2026-08-16 end of day: Phase 0 and Phase 1 (Bilateral Agreement + Authority Letter) both shipped and pushed to `origin/main` (`f7183ed`, 6 commits ahead of the last-pushed `bf39ad2`).** Full combined-range secrets sweep (`git diff bf39ad2..HEAD`) came back clean before pushing — no `Confidential Data/` PII, only fabricated placeholder names and already-public org/file names. Backend suite: **752 passed, 0 failed** (last measured; unchanged since, later work this session was read-only).

- **Live demo, done and real**: application `11189` (`TMA Khal Dir Lower .pdf`, bulk-uploaded through the actual running dev server against the real dev DB) reached `PENDING_REVIEW` automatically once OCR finished — the queue's `PipelineRunnerService` chains analysis → confidence → normalization → rule validation on its own, no manual per-stage calls needed. `overall_status: FAILED`, correctly: 5 of 8 required types are genuinely absent from this real file (`FORMAL_REQUEST_LETTER`, `SCHEDULE_OF_CHARGES`, `ACCOUNT_MAINTENANCE_CERTIFICATE`, `BILATERAL_AGREEMENT`, `TRIPARTITE_AGREEMENT`). `AUTHORITY_LETTER` came back `PARTIALLY_VERIFIED` with real fields extracted live (`focal_person_name`/`focal_person_designation`/`organization_name`), proving today's extractor end to end on real data, not just fixtures.
- **Honest-fallback behavior confirmed on this same real application**: `ONE_LINK_LETTER` and `BUSINESS_REQUIREMENT_DOCUMENT` (recognized checklist types, no extractor yet — `DocumentAnalysisService._persist_recognized_unsupported_type`) and `OTHER_SUPPORTING_DOCUMENT` (genuine catch-all, `_persist_undetermined_type`) all came back `NEEDS_REVIEW` with an honest message, never silently missing and never falsely approved.
- **Bilateral Agreement real-sample validation: CONFIRMED BLOCKER, still open as of 2026-08-17 (second search round).** First round: `GDA Abbotabad.pdf` (41 pages, ~2 hours to OCR) finished: 10 documents split out (`ACCOUNT_MAINTENANCE_CERTIFICATE` ×4, `AUTHORITY_LETTER` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `ONE_LINK_LETTER` ×3, `OTHER_SUPPORTING_DOCUMENT` ×1), zero `BILATERAL_AGREEMENT`, across the first **5 real files checked** (`Conservator Wildlife Peshawar Zoo.pdf` -- split-checked only, not full-text cached: see the 21-file splitting-verification entry above, 7 documents split and classified into 4 types, no `BILATERAL_AGREEMENT` among them; `DG Sports KP Onboarding Documents.pdf`, `TMA Khal Dir Lower .pdf`, `GDC Alpurai Shangla .pdf`, `GDA Abbotabad.pdf` -- all 4 fully OCR-cached end to end). **Second round, same day**: a 3-file split-only checkpoint (title/type classification only, not full OCR-text caching -- cheaper but still a real per-page OCR cost) worked through the untouched-file list in order -- `GDC Madyan Swat.pdf` (9 pages: `ACCOUNT_MAINTENANCE_CERTIFICATE` ×1, `ONE_LINK_LETTER` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1), `GDC Nathiagali Abbottabad.pdf` (10 pages: `AUTHORITY_LETTER` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1), `GGDC No 2 Haripur .pdf` (10 pages: `ACCOUNT_MAINTENANCE_CERTIFICATE` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1) -- again zero `BILATERAL_AGREEMENT`, and notably thin packets (only 3 documents split out per file, vs. 7-10 in the richer files checked earlier). That's 8 real files checked end to end now, zero Bilateral Agreement samples found in any of them. **Third round, next batch same investigation**: a further 3-file split-only checkpoint continued through the untouched-file list in order -- `GPGC Mardan.pdf` (10 pages: `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `ONE_LINK_LETTER` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1), `GPGC no 1 Abbottabad.pdf` (11 pages: `ACCOUNT_MAINTENANCE_CERTIFICATE` ×1, `AUTHORITY_LETTER` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `OTHER_SUPPORTING_DOCUMENT` ×1), `KP Workers Welfare Board.pdf` (23 pages, notably bigger than the rest of this batch: `AUTHORITY_LETTER` ×1, `BUSINESS_REQUIREMENT_DOCUMENT` ×1, `CNIC_FRONT` ×2, `OTHER_SUPPORTING_DOCUMENT` ×1) -- again zero `BILATERAL_AGREEMENT` in any of the three. **That's 11 real files checked end-to-end-or-split-checked now, zero Bilateral Agreement samples found anywhere.** Stopped deliberately at this checkpoint rather than continuing unprompted -- per-file OCR cost is real (up to ~2 hours for the largest file seen) and this session's demo deadline is time-sensitive, so continuing to search the remaining **10 of 21 real source PDFs** still genuinely unprocessed (`KP-PPRA Onboarding Documents.pdf`, `Onboarding Documents of KP-PSRA.pdf`, `TMA Lal Dir Upper.pdf`, `TMA Lal Qilla Dir Lower.pdf`, `TMA Samarbagh Dir Lower.pdf`, `TMA Thall, Hangu.pdf`, `TMA Thall Agreement.docx`, `UAD Mardan Onboarding File.pdf`, `WSSC Kohat Onboarding File.pdf`, `WSSC Mingora Swat Onboarding file.pdf` -- this list corrects the previous round's vaguer "three more TMA files, two WSSC files, one .docx" grouping, which silently omitted `TMA Thall, Hangu.pdf` and `UAD Mardan Onboarding File.pdf` entirely; the "13 of 21" count itself was arithmetically correct, only the accompanying named list was incomplete) is an explicit call for whoever owns the demo timeline, not something to keep doing automatically. `BilateralAgreementExtractor` (`08bec31`) is built and unit-tested against `docs/Master_Rules_Combined.md`'s spec and synthetic fixtures only; it has never been validated against a real sample, and none of the 11 files on hand contain one.
- **Formal Request Letter real-sample validation: CONFIRMED BLOCKER, found 2026-08-17 -- unlike Bilateral Agreement above, no extractor has been written for this type at all.** Checked *before* writing any code, per the real-sample-validation rule: zero `FORMAL_REQUEST_LETTER` hits anywhere in `Confidential Data/.ocr_cache/` across the 4 fully OCR-cached real sources (`DG Sports KP Onboarding Documents.pdf`, `TMA Khal Dir Lower .pdf`, `GDC Alpurai Shangla .pdf`, `GDA Abbotabad.pdf`); the 5th real source checked so far, `Conservator Wildlife Peshawar Zoo.pdf`, was never full-text cached (split-checked only -- see the Bilateral Agreement entry above), but its own classification list (21-file splitting-verification entry above: 7 documents across 4 types) independently confirms the same zero result for this file too, and `git log --all -S "FORMAL_REQUEST_LETTER" -- backend/app/document_analysis/` confirms no branch, ever, has touched this type in that module. **This is not a fully exhausted search**: `Confidential Data/` holds **21 real source PDFs total, and only these 5 have ever been split or OCR'd this session** -- the other 16 (`GDC Madyan Swat.pdf`, `GDC Nathiagali Abbottabad.pdf`, `GGDC No 2 Haripur .pdf`, `GPGC Mardan.pdf`, `GPGC no 1 Abbottabad.pdf`, `KP Workers Welfare Board.pdf`, `KP-PPRA Onboarding Documents.pdf`, `Onboarding Documents of KP-PSRA.pdf`, three more TMA files, two WSSC files, one `.docx`) have never been processed at all. Treat this as a standing, reportable gap, same tier as the Bilateral Agreement blocker above -- **no `FormalRequestLetterExtractor` should be written until a real sample turns up**, either by OCRing one of the 16 untouched files (real CPU cost, minutes/page on this machine) or a manually-sourced sample. **Correction, 2026-08-18: a real sample has now been found -- see the dedicated entry near the end of this section.** The "zero hits anywhere" premise no longer holds; the "no extractor exists yet" half of this blocker is unchanged.
- **[SUPERSEDED 2026-08-22 -- see the dated decision entry near the end of this section] Schedule of Charges real-sample validation: CONFIRMED BLOCKER, found 2026-08-17 -- same investigation, no extractor written.** Same checks as Formal Request Letter, same result: zero `SCHEDULE_OF_CHARGES` hits in `Confidential Data/.ocr_cache/` across the 4 fully OCR-cached sources (see the same Conservator Wildlife Peshawar Zoo.pdf split-checked-only caveat documented in the Formal Request Letter entry above -- that file's own classification list independently confirms the same zero result), and `git log --all -S "SCHEDULE_OF_CHARGES" -- backend/app/document_analysis/` shows no branch has ever touched this type in that module. This is consistent with, not just asserted alongside, two things already on record above: the live-demo application (`11189`, `TMA Khal Dir Lower .pdf`) listed `SCHEDULE_OF_CHARGES` as one of the "5 of 8 required types genuinely absent from this real file," and the first-two-files splitting job found only 4 types anywhere in `Conservator Wildlife Peshawar Zoo.pdf`/`DG Sports KP Onboarding Documents.pdf` (`ACCOUNT_MAINTENANCE_CERTIFICATE`, `AUTHORITY_LETTER`, `CNIC_FRONT`, `BUSINESS_REQUIREMENT_DOCUMENT`), i.e. not this type either. Same **not a fully exhausted search** caveat as Formal Request Letter -- the same 16 of 21 real source PDFs remain unprocessed. **No `ScheduleOfChargesExtractor` should be written until a real sample turns up.** One forward-looking clue, flagged as speculation, not a finding: `upload/constants.py::MAX_COPIES_BY_DOCUMENT_TYPE` allows up to 6 copies of this type (the highest cap of any required document), which is suggestive of the real documents arriving per-service rather than once per application -- worth keeping in mind when a sample does surface, not something to design around yet.
- **CNIC scope decision made 2026-08-17: build front-only extraction now.** `CnicFrontExtractor` (Phase 1, sixth checklist type) is built and real-sample validated -- see the findings entry directly below for the facts that informed the decision. `CNIC_BACK` stays deliberately unmapped: zero real `CNIC_BACK` samples exist anywhere, so back-face extraction has no real-sample validation and remains out of scope until one turns up; a back upload still falls through to the generic `ID_DOCUMENT` classifier exactly as before. Fields: `document_number` (critical, format-anchored on the canonical `NNNNN-NNNNNNN-N` shape with no label dependency, so it's position-independent -- extracts correctly on all 3 real samples including the one with scrambled OCR read order), `full_name` (non-critical -- reliable across all 3 real samples but validated against only one real organization), `date_of_expiry` (non-critical -- anchored on an exact two-label/two-value block that holds on 2 of 3 real samples, honestly misses on the scrambled one rather than guessing). `document_number` deliberately reuses the same field name `IdentityExtractor` already produces, so `FormatCnicRule` (`rule_engine/rules/format_rules.py`) validates it automatically, no new rule needed. **Real-sample validation result, run directly against all 3 real cached samples**: 2 of 3 (the clean-layout ones, including the one with the unrelated splitter-merge-artifact content trailing after the CNIC portion) come back fully `VERIFIED` (all 3 fields present); the scrambled-order sample comes back `PARTIALLY_VERIFIED` (`document_number`/`full_name` present, `date_of_expiry` honestly missing) -- matching designed behavior exactly, not a surprise. `father_name`/`date_of_birth`/`date_of_issue` were not attempted this pass, scoped to what docs/Master_Rules_Combined.md Section 12 asks for by name (format, expiry, readability, consistency) rather than extracting every field the layout partially allows. The splitter merge-artifact bug noted below is unaffected by this change -- still not investigated, still a real bug worth its own look.
- **CNIC (front/back) findings, gathered 2026-08-17, informed the scope decision directly above.** Before this fix, `CNIC_FRONT`/`CNIC_BACK` were not `AnalyzedDocumentType` members at all (confirmed by grep, zero hits in `document_analysis/constants.py`/`extractors.py`) and were not in `_CHECKLIST_TYPE_MAP` -- no analysis-layer slot, no presence rule, no completeness check existed for CNIC anywhere in the codebase. **Unlike every other unbuilt checklist type, a real CNIC upload did not honestly fall to `NEEDS_REVIEW` -- it got silently misclassified.** Because `CNIC_FRONT` wasn't in `_CHECKLIST_TYPE_MAP`, it fell through to the generic keyword classifier (`detect_document_type`) instead of being intercepted first; unlike Schedule of Charges/Formal Request Letter/1-Link Letter's real content, CNIC's real content does score positively against that classifier's `ID_DOCUMENT` keyword table (`"identity card"` weight 3, `"date of birth"` weight 2). Empirically confirmed by running `detect_document_type`/`extract_fields` directly against all 3 real cached samples before the fix: **all 3 classified as `ID_DOCUMENT` and ran through the generic `IdentityExtractor`**, persisting as `document_type: "ID_DOCUMENT"`, not `"CNIC_FRONT"` -- this is now fixed for front-facing uploads by `CnicFrontExtractor` above; back-facing uploads still hit this exact misclassification, since `CNIC_BACK` deliberately stays unmapped. **Real samples**: 3 real `CNIC_FRONT` samples exist in `Confidential Data/.ocr_cache/`, all from one organization (`DG_Sports_KP_Onboarding_Documents__CNIC_FRONT_copy1/2/3.txt`); **zero `CNIC_BACK` samples exist anywhere in the cache.** Two of the three front samples (~30 lines each) are clean, standard, clearly labeled CNIC fronts. **The third (~1,036 lines / ~40KB) is a genuine splitter merge-artifact**: the first ~29 lines match the same clean CNIC-front structure as the other two, then a page-break marker appears, then ~1,000 lines of unrelated organizational-registration content (organization name/registration number, nature of organization, executive committee members, bank name/branch) is merged into the same document boundary. **Flagging this as a real splitting bug worth its own investigation, independent of the CNIC extraction work above** -- it's structurally similar in *shape* to the checklist/manifest cover-page false positive already fixed in `68d47d6` (unrelated content bleeding across a page boundary into the wrong document), but **the actual root cause was not checked this session** -- don't assume it's the same mechanism until someone reads the raw splitter output around that boundary. `CnicFrontExtractor`'s patterns are anchored on the CNIC-front portion only, so the trailing contamination did not need to be worked around to validate successfully against that sample -- but the bug itself is still unfixed and still worth a look. **Resolved, 2026-08-18: root cause found -- the ~1,000 lines of "unrelated organizational-registration content" are the genuine 1-Link Application Form** (docs/Master_Rules_Combined.md Section 4), silently absorbed as a continuation because its own title never matched any `ONE_LINK_LETTER` phrase. See the splitter fix entry near the end of this section -- fixing that phrase gap also splits this sample cleanly, closing this bug as a side effect.
- **2026-08-17: the "1-Link Letter" checklist slot is not populated by what docs/Master_Rules_Combined.md Section 4 describes -- flagged prominently, needs a product/ops decision, not a code fix.** `DocumentType.ONE_LINK_LETTER` now has a real extractor (`OneLinkLetterExtractor`), but built against what's actually there: **every one of the 4 real cached samples (Confidential Data/.ocr_cache/, 2 independent organizations) is a signed "PARTICIPATION MEMORANDUM FOR BILLER/SUB-BILLERS/BILL AGGREGATOR MEMBERS,"** matching Section 8's "E-Stamp Papers & Notary Public (Participation Memorandum & SLA)" content and Section 6 Tripartite Agreement's three-party/witness shape. **None contain Section 4's actual required content** -- Organization Name/NTN/Country of incorporation/Proprietor-Directors-Partners-with-CNICs/Nature-of-Business classification/license details/Account Match check. The genuine 1-Link Application Form has never turned up in any real file processed this session. **Correction, 2026-08-18: it has now -- see the dedicated entry near the end of this section.** Practical effect: **`ONE_LINK_LETTER` verification today checks Participation Memorandum content (organization name, one non-critical branch-code field), not Section 4's literal requirement** -- real applications will show `VERIFIED`/`PARTIALLY_VERIFIED` against a document that legally may not be what the checklist item is meant to require at all. Root cause found, not fixed: `app/preprocessing/splitter.py`'s `ONE_LINK_LETTER` title-match list mixes the correct strong phrase ("1LINK APPLICATION FORM") with bare fallback keywords ("1LINK", "ONE-LINK", "ONELINK") that also appear constantly in Participation Memorandum boilerplate, plausibly weak-matching it into this slot instead. **This is a scope question for whoever owns the department/checklist relationship, not something this session is resolving in code** -- either the checklist item's real-world meaning needs correcting, or the splitter needs tightening to stop conflating the two documents, or both.
- **2026-08-17: latent bug found in `confidence/services.py` while validating the 1-Link Letter extractor's test suite -- flagged, not fixed, out of scope for Phase 1.** `ConfidenceService.evaluate()`'s completion log line (`services.py:337-346`) formats `overall` with `%.3f`, but `overall` comes from `_overall_confidence(entries)` (`services.py:523`), typed `float | None` and genuinely `None` whenever `entries` is empty -- i.e. an application whose analyzed documents collectively produced zero extracted fields. `str.__mod__`/`logging`'s `%`-formatting raises `TypeError: must be real number, not NoneType` on `None`, which is unhandled, so `POST /confidence/evaluate` 500s instead of returning a valid (if degenerate) response for that application. Found because `test_confidence_api.py::add_scanned_statement` (reused by `test_feedback_api.py`, `test_normalization_api.py`, `test_rule_engine_api.py` -- 20 test failures total, see the git history around this entry's commit) fed `BANK_STATEMENT_TEXT` through a document type (`ONE_LINK_LETTER`) that had just gained a real extractor it doesn't match, producing exactly this zero-fields case; fixed the test fixture (swapped to `DocumentType.SCHEDULE_OF_CHARGES`, still unmapped in `_CHECKLIST_TYPE_MAP`) rather than the production code, since the production bug is real but predates this session's changes and reproducing it doesn't require any document type in particular -- any application whose every document analyzes to zero fields hits it. Needs its own fix (e.g. guard the log call, or have `_overall_confidence` return `0.0` instead of `None`) plus a regression test; not attempted here to keep the 1-Link Letter commit scoped to what it was reviewed for. **Resolved, 2026-08-18 (`e7b475a`), confirmed still passing 2026-08-19**: guarded the log line rather than changing `_overall_confidence` to return `0.0` -- `None` is the deliberate, documented "nothing was scored" contract (`EvaluateResponse.overall_confidence: float | None`, same None-not-zero convention `reports/services.py` independently uses), so collapsing it to `0.0` would misrepresent an unscored application as a zero-confidence one. Regression test `test_evaluate_with_zero_extracted_fields_does_not_500` added (`caplog.set_level(logging.INFO)` is required, not decorative -- pytest's default WARNING capture level otherwise leaves the crash dormant during a normal test run).
- **Process lesson, 2026-08-17: a shared test fixture that uses a `DocumentType` as a generic "no real extractor yet" placeholder breaks every test file that reuses it, not just the extractor's own test file.** This happened three separate times this session, each time only found by actually running the full suite rather than the newly-touched file in isolation: (1) BRD -- `test_reports_api.py`'s `build_full_application` fed `BANK_STATEMENT_TEXT` through `DocumentType.BUSINESS_REQUIREMENT_DOCUMENT` as a stand-in for "any required type with no extractor," broke once BRD got one; (2) 1-Link Letter -- `test_document_analysis_api.py`'s `test_analyze_payslip_from_scanned_image` used `DocumentType.ONE_LINK_LETTER` the same way, single-test blast radius; (3) 1-Link Letter again, same session -- `test_confidence_api.py`'s `add_scanned_statement` used the identical pattern, but this helper is imported and reused by `test_feedback_api.py`, `test_normalization_api.py` and `test_rule_engine_api.py`, so one placeholder-type choice cascaded into 20 failures across four files at once, plus surfaced the `confidence/services.py` latent bug directly above as a side effect. **Recommended practice for any future extractor added to `_CHECKLIST_TYPE_MAP`** (next candidates: Formal Request Letter, Schedule of Charges, CNIC front/back): `grep -rn "DocumentType\.<TYPE>" backend/tests/` before wiring the type in, not just re-running that type's own test file -- a placeholder usage anywhere in `tests/` is a future break, and shared helpers (anything imported by more than one test file) carry a wider blast radius than a local fixture does.
- **Teammate bug triage, corrected 2026-08-17**: a teammate's independent review (32 reported issues) was verified read-only against the current codebase. First-pass tally was 29 confirmed / 3 false; **Critical #1 ("missing auth on 40+ endpoints") was then found to be a false positive on a second pass** — the original grep checked each route file individually for `_CURRENT_USER` and missed that `app/api/__init__.py` already wraps every flagged router in a single `protected_router = APIRouter(dependencies=[Depends(get_current_user)])`, applied globally on 2026-08-14 (see `tests/test_auth_enforcement.py`, which passes 3/3 and empirically confirms all 53 protected endpoints reject unauthenticated requests today). **Corrected tally: 28 confirmed, 4 false, 0 unverifiable.** Full findings and the correction note in `docs/TEAMMATE_BUG_TRIAGE.md`; none of the 28 confirmed items have been fixed yet, pending an explicit scope decision. Lesson for any future automated code-review pass: a per-file grep for a dependency can miss centralized/router-level wiring — check the aggregation point (`app/api/__init__.py` here) before concluding a dependency is absent.
- No `origin/track-a-*`/`origin/track-b-*` branches exist yet (checked repeatedly through the session).
- **Full-stack trust audit, 2026-08-17** — every claim on record above re-derived from the actual repo state (git, both codebases, the contract between them, both test suites, all docs), not assumed from this file. Full findings, all four buckets, in `docs/FULL_STACK_AUDIT_2026-08-17.md`.
- **2026-08-18: `TMA Lal Dir Upper .pdf` fully OCR'd (9 pages, full pipeline) -- confirms the audit's B.9 continuation-absorption limitation on a second real file, and casts doubt on every prior split-only "zero found" search.** Two human-identified page ranges (pages 4-6, pages 8-9) were checked against the actual OCR'd text, not assumed from the page numbers alone. The splitter itself produced only **2** documents from this file's 9 pages: page 1 (a checklist cover page, correctly excluded and typed `OTHER_SUPPORTING_DOCUMENT` by the existing `68d47d6` fix) and a single 8-page blob typed `AUTHORITY_LETTER`. Reading that blob's actual content shows it silently contains **5 distinct real document types**, not one: a genuine Authority Letter (page 2, correctly the one page that triggered the strong title match), a bank-account-details page (page 3), a 3-page 1-Link Application Form (pages 4-6, see below), a Formal Request Letter (page 7, see below), and a Participation Memorandum (pages 8-9, see the Tripartite Agreement entry below) -- none of the latter 7 pages independently triggered a header-zone strong-title match, so each was silently absorbed as a "continuation" of whatever document came before it, exactly the false-negative mechanism the audit (`docs/FULL_STACK_AUDIT_2026-08-17.md`, B.9) identified by reading the splitter's code but had not yet confirmed against a live example. **Methodology caveat, applies retroactively**: every split-only "zero `BILATERAL_AGREEMENT`/`SCHEDULE_OF_CHARGES`/etc. found" conclusion in this file (the 11-file Bilateral Agreement search above, the Formal Request Letter and Schedule of Charges blocker entries) trusted the splitter's own type label as ground truth for "this file doesn't contain that type." This file proves that assumption can be wrong -- a real Formal Request Letter and a real Tripartite-shaped document were both present in a file whose split-only classification would have reported neither. **Not asserting the prior 11-file search's zero results are wrong** -- no re-check was done here, only this one new file -- only that they can no longer be treated as conclusive proof of absence, just as "not independently title-matched." Worth a real re-check with full OCR-text reads (not split-only counts) before treating any of those "zero" conclusions as final.
- **2026-08-18: the genuine 1-Link Application Form (docs/Master_Rules_Combined.md Section 4) has now turned up in a real file -- corrects the "never turned up" claim in the 2026-08-17 entry above.** `TMA Lal Dir Upper .pdf` pages 4-6 (human-identified, confirmed by direct OCR read) are a genuine "Application Form (In-Direct Customer)" package -- Form A (Organization Name, NTN/Registration No., Country of Incorporation, Bank Account Number, Title of Account) on page 4, a continuation with the license-status table, Politically-Exposed-Person/foreign-national declarations, and the Proprietor/Directors/Partners/Trustees table (name, CNIC, nationality, function) on page 5, and Form B (business-model details, transaction volumes, declarations, signature blocks) on page 6 -- matching Section 4's spec field-for-field, the first time this shape has appeared in any real file processed this session (previously 4/4 samples, 2 orgs, were all Participation Memorandum content instead, see above). **The splitter did not independently classify these 3 pages at all** -- see the absorption entry above; they were silently absorbed into the `AUTHORITY_LETTER` blob. **`OneLinkLetterExtractor` run directly against this content extracts nothing** (`organization_name`/`branch_code` both empty -- confirmed empirically, not assumed): its patterns are anchored on Participation Memorandum boilerplate phrasing ("<ORG> hereby authorizes 1LINK to take actions") that does not appear anywhere in the genuine Application Form's text. **This changes the shape of the standing 1-Link Letter scope question**: it is not simply "the splitter always mis-routes the same wrong document into this slot" -- different real onboarding packages contain genuinely different documents for the same checklist item (Participation Memorandum from 2 orgs across 4 samples; the real Application Form from a 3rd org here). Any future fix needs to handle both shapes, or the checklist/splitter needs a way to tell them apart, not just point at one as "the real one." No code changed this step, per instructions -- flagged for a product/ops scope decision, same as the entry above.
- **2026-08-18: Tripartite Agreement's first-ever real-sample validation -- partial match, not fully validated.** `TMA Lal Dir Upper .pdf` pages 8-9 (human-identified as Tripartite Agreement, confirmed by direct OCR read) are the same "PARTICIPATION MEMORANDUM FOR BILLER/SUB-BILLERS/BILL AGGREGATOR MEMBERS" pattern already documented above under the 1-Link Letter mismatch (clause-numbered (i)-(xii), three named parties, witness sections) -- confirming that in practice this document substantively fulfills both checklist slots' real-world content, in different files. **The splitter did not independently classify these 2 pages either** -- absorbed into the same `AUTHORITY_LETTER` blob (see the absorption entry above). `TripartiteAgreementExtractor` (`08bec31`, unit-tested against the spec and synthetic fixtures only until now) run directly against this content: **3 of 6 fields extract correctly** (`party_1link`, `party_kpitb`, `party_subbiller` -- all match the real named parties: 1LINK, Khyber Pakhtunkhwa Information Technology Board, and the sub-biller department). **2 of 6 garbage-capture** -- `account_holder` captures the bank-details table's column header ("IBAN/Account No") instead of the real account title, and `account_number` captures the row index ("01") instead of the real account number; both patterns assume a same-line "Label: value" shape, but the real document's bank details are a multi-column table where headers and values land in different OCR lines -- the same failure class as the already-documented AMC `/IBAN` garbage-capture bug. **1 of 6 honestly misses** -- `branch_code`'s pattern requires a colon-delimited "Branch:"/"Branch Code:" label; the real sample states the branch parenthetically ("Branch (0312)"), the same shape `OneLinkLetterExtractor`'s own `branch_code` pattern was already built to expect elsewhere. **Honest verdict: first real validation, 3/6 correct, 2/6 silently wrong (worth prioritizing over the 1 honest miss, since a garbage value can pass downstream as if it were real data), 1/6 honest miss.** Not rounding this up to "validated" -- the two garbage-capture fields need fixing before this extractor's output can be trusted on real Tripartite Agreement data.
- **2026-08-18: Formal Request Letter's first real sample found -- corrects the "zero hits anywhere" claim in the 2026-08-17 blocker entry above; the "no extractor exists" half was closed the same day, see the dedicated entry directly below.** `TMA Lal Dir Upper .pdf` page 7 (found while reading the absorbed `AUTHORITY_LETTER` blob above, not independently classified by the splitter) is a genuine formal letter to "The Managing Director, KPIT Board" requesting a digital account for online payments -- substantively matches Section 11's requirement (a formal request explicitly asking KPITB to onboard the organization), though its literal subject line ("REQUEST FOR DIGITAL ACCOUNT AND FOR ONLINE PAYMENTS") differs from the master rules' suggested example phrasing ("Onboarding as a sub-biller with KPITB"). This is a real sample now sitting in `Confidential Data/.ocr_cache/` (inside the `AUTHORITY_LETTER_copy1` cache file, not its own).
- **2026-08-18: `FormalRequestLetterExtractor` added (commit `3770e2c`), closing the "no extractor exists" half of the blocker above -- undocumented in this file until now (found 2026-08-22 while auditing for missing entries).** Built against the one real sample above, all 3 fields honestly scoped to what that one sample actually supports: `organization_name` originally captured only the office-holder title on the anchor's own line ("OFFICE OF THE <title>") instead of the real organization name one line below it -- pattern now explicitly discards the anchor's own line and captures the next one; not validated against any other anchor variant (DEPARTMENT OF/GOVERNMENT OF/TO THE/FROM) since none has a real sample yet, so that shape is assumed to generalize, not confirmed. `focal_person_designation` previously bled across a line boundary (a bare "Title" inside the real sample's "Account Title" table header, no colon, satisfied the old permissive gap and crossed into the next line's unrelated "IBAN/Account No" content) -- now requires an explicit `:`/`|`/`-` delimiter on the same line, correctly leaving the field missing on this sample (it never states a designation for its focal person) rather than papering over it. `date` is a known, still-open honest miss on the one real sample: the real text reads "Dated Dir(U) 13/07/2026" (unrelated words between label and date) and separately "Dated: 14-04-2026" referring to a third-party letter being cited, not this letter's own date -- neither fits the strict label-then-date shape, and matching the second would be actively wrong. Only 1 real sample exists for this type total -- same single-organization caveat as every other extractor built against exactly one real file.
- **2026-08-18: splitter fix -- the genuine 1-Link Application Form's own title now correctly starts a document, instead of being silently absorbed.** Root-cause diagnosis of the 3 missed boundaries in `TMA Lal Dir Upper .pdf` (entries above) found one safely fixable case: `_STRONG_TITLE_PHRASES`'s `ONE_LINK_LETTER` entry only listed brand-prefixed variants ("1LINK APPLICATION FORM", "ONE-LINK", "1LINK", etc.), but the real form's own page title is "Application Form (In-Direct Customer)" with no brand prefix at all -- a pure wording mismatch, not a position or missing-entry issue (confirmed at line 0 of its page, well inside the header zone). **Evidence base is n=4, not n=1**: checking this candidate phrase against every already-cached real sample (before applying it) found the same real title, previously unidentified, already hiding inside 3 other cached documents from 2 other files -- see the CNIC merge-artifact resolution above (`DG_Sports…CNIC_FRONT_copy2`), plus `GDA_Abbotabad…ACCOUNT_MAINTENANCE_CERTIFICATE_copy4` and `TMA_Khal_Dir_Lower…AUTHORITY_LETTER_copy2`. Fixed by adding `"APPLICATION FORM (IN-DIRECT CUSTOMER)"` to the phrase list (`app/preprocessing/splitter.py`). **Compatibility-checked before applying, not assumed safe**: truncating each of the 3 already-cached samples at the new boundary and re-running their current extractor showed zero regression for `CnicFrontExtractor` and `AuthorityLetterExtractor` (identical fields and values). **One real, accepted regression**: `AccountMaintenanceCertificateExtractor` on `GDA_Abbotabad…ACCOUNT_MAINTENANCE_CERTIFICATE_copy4` loses `branch_name` -- that field's value was being pulled from the absorbed Application Form content, not the genuine AMC portion, so losing it is a correction, not a loss of real data; `branch_name` is not in AMC's `CRITICAL_FIELDS`, so this downgrades that one sample from fully-`VERIFIED` to missing one non-critical field, not a new blocker. **Correction, same day: the "3 separate copies" side effect noted here was itself wrong, and has been fixed.** It assumed the form is always exactly 3 pages (true for 3 of the then-known samples, but a 4th -- `GDA_Abbotabad…ACCOUNT_MAINTENANCE_CERTIFICATE_copy4` -- turned out to be 4 pages), and it produced a real regression: `TMA Khal Dir Lower .pdf`'s 3 form pages plus 1 pre-existing genuine `ONE_LINK_LETTER` sample totaled 4, exceeding `MAX_COPIES_BY_DOCUMENT_TYPE`'s cap of 3 and hard-failing that file's entire bulk upload. Fixed via a new `_CONTINUATION_TITLE_PHRASES` mechanism in `splitter.py`: a repeat of this exact phrase on the immediately following strong-matched page now extends the current document instead of starting a new copy -- page-count-agnostic by design (confirmed against both the 3-page and 4-page real shapes), and deliberately scoped to only this phrase, so the brand-prefixed `"1LINK APPLICATION FORM"` phrase's existing repeated-copies behavior (genuinely separate real forms, per this constant's own documented intent) is untouched. Regression tests updated accordingly (`tests/preprocessing/test_document_splitter.py`): the 3-page and 4-page cases both now assert exactly 1 document, plus a new test confirming the group still ends cleanly before a genuinely different document. **The other 2 of tonight's 3 missed boundaries were left unfixed, genuinely inconclusive**: Formal Request Letter's real subject line is idiosyncratic to one sample (n=1, no basis to generalize a phrase); the Participation Memorandum/Tripartite boundary's only candidate phrase ("PARTICIPATION MEMORANDUM") was confirmed, not assumed, to collide with 3 of the 4 already-working real `ONE_LINK_LETTER` samples (it would out-rank their current matching phrase in table order), and its position also independently fails the header-zone cutoff on this file regardless. Regression tests added (`tests/preprocessing/test_document_splitter.py`, synthetic fixtures, no real PII) covering both the new case and the documented 3-copies side effect. Full suite: 767 passed, 13 failed -- same 13 named failures as the established baseline, zero regressions.
- **2026-08-18: Bilateral Agreement / Tripartite Agreement splitting -- confirmed a wrong-mechanism problem, not a missing-phrase one. Stop chasing this with text-phrase changes.** Investigated whether the same phrase-table approach that fixed the 1-Link Application Form (above) could also detect these two types, before writing any code. Re-read `docs/Master_Rules_Combined.md` Sections 4, 6, 7, 8 directly rather than inferring from one real file: Section 8, "E-Stamp Papers & Notary Public," is explicitly titled *"(Participation Memorandum & SLA)"* -- naming Participation Memorandum and SLA (Bilateral Agreement, per Section 7's own title) as the two types its requirements govern. Its actual authenticating signal is **Visual Authentication** (the print must resemble genuine E-stamp paper texture/color, not plain A4) and **Watermark** validity -- both physical/visual properties, plus Notary Public stamp/seal/photo requirements, also visual. **Confirmed empirically, not just from the spec text**: the literal phrases "TRIPARTITE AGREEMENT"/"BILATERAL AGREEMENT" never appear in any real cached sample (already known); the more specific stamp-paper boilerplate text ("PRINT STAMP PAPER - VENDOR", "NON-JUDICIAL") does leave *some* OCR trace in 3 real files, but repeats across **6 separately-timestamped pages** in one real file (`DG Sports…`), almost certainly 6 separate verification-portal screenshots (consistent with the checklist's own "3 Sets" stamp-paper requirement for both this type and Schedule of Charges) rather than one page per logical agreement -- using it as a splitter boundary would risk the same over-fragmentation problem just fixed for the Application Form, likely worse, on top of visibly worse OCR quality on these specific pages. **Conclusion: even a perfect text-based splitter could only ever route this content to the right slot -- it could never itself satisfy Section 8's actual requirement, since that requirement is defined as a visual property no OCR text pipeline can verify.** No further attempts to fix this by adding or adjusting `_STRONG_TITLE_PHRASES` entries should be made -- real detection needs a different mechanism, closer to Phase 2's visual verification work (image-level stamp-paper color/texture/watermark detection), not a text phrase. Left undone, deliberately, as a scope boundary rather than an open bug to keep chasing.
- **2026-08-18: `DGAM_Onboarding_file.pdf` investigated -- a new file, not part of the original 21-file inventory above.** 17 pages. Split-only classification pass (cheap, no full-text caching): **3 documents** -- `ACCOUNT_MAINTENANCE_CERTIFICATE` x1, `AUTHORITY_LETTER` x1, `OTHER_SUPPORTING_DOCUMENT` x1. No `BILATERAL_AGREEMENT`/`SCHEDULE_OF_CHARGES`/`FORMAL_REQUEST_LETTER`/`TRIPARTITE_AGREEMENT`/`ONE_LINK_LETTER` label present. **Given the absorption-bug finding confirmed the same day on `TMA Lal Dir Upper .pdf` (above) -- where 8 of 9 pages containing 5 distinct real document types were compressed into a single label -- this "zero blocked types" result cannot be treated as reliable evidence of absence, only as "the splitter didn't independently title-match any of them."** 17 pages into 3 buckets is an even higher compression ratio than TMA Lal Dir Upper's. A full OCR-cache read (particularly of the `OTHER_SUPPORTING_DOCUMENT` bucket, the most likely dumping ground for anything that didn't strong-title-match) would be needed to actually confirm or rule out hidden content -- **deliberately not run yet, stopping here to report rather than spending that OCR cost unprompted.** Separately, whether this file's manual number redaction shows up as blank/garbled OCR (relevant to whether it's useful as an extraction sample vs. a structure-only reference) also could not be checked without that same full OCR pass -- also unanswered, pending a go-ahead.
- **2026-08-18, frontend session: role-specific UI shipped for Employee/Operator/Reviewer/IT, no backend changes. Frontend suite: 64 tests, 11 files, all green (`npm test`); `npm run build` clean; `npm run lint` 0 errors (4 pre-existing fast-refresh warnings).** Full role matrix (per user): Employee = everything; Operator = Applications, Processing, Validation, Settings; Reviewer = Applications, Processing, Validation Report, Human Review, Settings; IT = Applications, Processing, Settings + System Logs. Dashboard Quick Actions are role-aware; Settings admin section (Feedback/Continuous Learning/System Logs) shows for Employee-or-IT only; System Logs stays link-visible for Employee but the page itself returns the backend 403 / Access Denied (IT-only endpoint, authoritative). Implementation notes: `roles.js` adds `isEmployee()` (raw role not in {OPERATOR,REVIEWER,IT}) and `visibleNavigationFor(user)` (deep-filtered NAVIGATION copy); `navigation.js` nav items now carry a `roles` array (validation→OPERATOR, reports/human-review→REVIEWER, admin children→IT); `Sidebar.jsx` renders the filtered nav. Validation page gained `ValidationQueueSummary` counter cards (Pending/Missing documents/Needs attention/Rejected/Completed, exactly-one-bucket priority order) and the detail panel now separates a received-checklist + missing-checklist with an "All required documents received." complete state. Validation Report terminology de-technicalized (no OCR/extraction/confidence jargon: "Document quality", "Fields & Standardization", "Field Review Progress", "Needs attention" column) plus an `[Open Human Review]` button (hidden for OPERATOR) that deep-links `?application=<id>` which `useHumanReview` now pre-selects. System Logs gained a free-text event-type filter, prev→next Status column, expandable JSON details rows, and offset pagination (`useSystemLogs` now pages at 50/entry via `{offset,limit}`). `apiError.js` got a 403 fallback message. Test files: new `roles.test.js`, `QuickActions.test.jsx`, `ValidationQueueSummary.test.jsx`; updated `SystemLogsPage/ValidationPage/SettingsPage` tests. Known: seeded Employee account (`Verification Officer`) clicking IT-only endpoints 403s by design (Access Denied page) -- documented limitation, no backend change made. Nothing committed (user to review).

- **2026-08-18, later same day, dashboard role-specific session: Quick Actions and welcome description are now genuinely role-specific; frontend suite 69 tests, 12 files, all green (`npm test`); `npm run build` clean; `npm run lint` 0 errors (same 4 pre-existing fast-refresh warnings).** Per user's per-role spec: **Employee** sees all 6 actions (New Application, View Applications, Open Validation, Open Validation Report, Open Human Review, System Logs); **Operator** sees New Application, View Applications, Open Validation, Processing; **Reviewer** sees View Applications, Open Validation Report, Open Human Review, Processing — **no longer the previous bug of seeing "New Application"** (Reviewer can't create apps); **IT** sees View Applications, Processing, System Logs. Each action has a role-specific description (e.g. Employee View Applications = "Browse and manage existing applications." vs Reviewer = "Browse applications assigned for verification."), and the Dashboard welcome subtitle is now role-specific (heading "Welcome back!" unchanged; Employee = "Here's an overview of your financial document verification workspace.", Operator = "Manage document intake, completeness checks, and application processing.", Reviewer = "Review validated applications and make final verification decisions.", IT = "Monitor application processing and system activity."). Implementation: single source of truth `getDashboardActions(user)`/`getDashboardWelcome(user)` in `src/data/dashboard.js` (`DASHBOARD_ACTIONS`/`DASHBOARD_WELCOME` maps, Employee precedence, OPERATOR fallback — QuickActions renders one config, no per-role JSX duplication); `QuickActions.jsx` and `Dashboard.jsx` both slimmed to consume the config; `RecentApplications` empty-state "Create New Application" button now gated to Employee/Operator only. New tests: `src/__tests__/components/dashboard/QuickActions.test.jsx` (full 4-role matrix + no-role OPERATOR fallback; moved from the old `__tests__/components/QuickActions.test.jsx`, which was removed) and `src/__tests__/pages/Dashboard.test.jsx` (welcome text per role + shared heading; child sections mocked). **Also added `ocr_cache/` and `.ocr_cache/` to `.gitignore`** — `docs/ocr_cache/` was holding untracked real OCR'd document text files (same confidentiality class as `Confidential Data/`); `Confidential Data/.ocr_cache` was already covered by the `/Confidential Data/` rule, now explicit too. Nothing committed (user to review).

- **2026-08-18, late session: Tripartite + AMC garbage-capture bugs fixed with a structural bank-account-block parser (`backend/app/document_analysis/extractors.py`).** The two extractors' `account_holder`/`account_number`/`iban` values came from label-anchored greedy regexes that produced confirmed garbage on the real cache: Tripartite (`TMA_Lal_Dir_Upper__ONE_LINK_LETTER_copy2.txt`, the stacked column table) captured the header `IBAN/Account No` as account_holder and the row index `01` as account_number; AMC copy1 (Allied) captured `/IBAN` as account_number (the documented `/IBAN` bug); AMC copy2 (ZTBL) leaked the page-2 NRSP IBAN-shaped value into account_number because `ACCOUNT NUMBER:...` dotted leaders broke the regex separator; AMC copy3 (NBP) truncated the wrapped 3-line account title. **The fix is a shared structural parser, not more regexes**: `_extract_bank_account_block(text)` runs two passes — a stacked column-block pass (header block positionally mapped onto the value block by *recognized* header, skipping OCR-noise header lines like the real `IENT` column, with the account-number slot validated as structurally real before the block is accepted) and an interleaved label/value pass (same-line `Label: value`, dotted-leader `Label:... value`, and bare-label-then-next-line values with wrapped multi-line titles capped at 3 lines and stopped before the next field's label). Both passes emit `(key, value, line_index)` captures merged per field by earliest occurrence — so first-match-in-document-order makes the ZTBL page-1 account number win over the page-2 NRSP value. Normalization is shape-guarded, not blacklist-guarded: all-digit values of length ≤ 3 (row indexes 01/02/03...) are rejected, values that are themselves known field labels are rejected, combined `account/IBAN` values are split, parenthetical IBAN tails are stripped (bonus: AMC copy4/BOK now yields `iban` from `3005642921(PK06…)`, previously not extractable), and Tripartite's IBAN-only account slot is promoted to `account_number` only when no plain account number exists anywhere in the document. `AccountMaintenanceCertificateExtractor` and `TripartiteAgreementExtractor` now override `extract()` to merge the block; both keep their other patterns (bank_name/branch_name/issue_date, party_* / branch_code) untouched, and the Tripartite merge deliberately excludes `iban` (not in its `EXPECTED_FIELDS`). **Verified AFTER against the same real cache (structural, values masked in this file):** Tripartite now extracts the real account title and the `PK44…` IBAN-shaped account value (was header/row-index garbage); all 4 AMC copies extract the correct account_holder (incl. the full wrapped NBP title) and account_number, with no `/IBAN`, header, row-index or page-2 leaks. **7 new synthetic (fabricated, `PK99FAKE…`) regression tests** in `test_document_analysis_engine.py` cover: column-block positional mapping + noise header, dotted leaders, combined account/IBAN split, wrapped multi-line title + plain-account-number-vs-IBAN disambiguation, page-1-first-match semantics, row-index guard, and honest-miss (absent) fields; 6 of 7 verified (via `git stash`) to genuinely fail on the pre-fix code. A real `PK44…`/`30901040400584`-style value caught in an early docstring was scrubbed to structural placeholders before anything was left in the diff — diff is PII-clean. **Test runs** (all against the isolated `finance_verification_test` DB — note: the current `tests/conftest.py` already redirects `DATABASE_URL` to `*_test` before any app import, so the old CONTEXT.md pytest-wipes-dev-DB warning does NOT apply to the current conftest): `test_document_analysis_engine.py` **66 passed** (59 pre-existing + 7 new); `test_document_analysis_api.py` 76 passed + **13 failed — the same documented `TECH_BLANK_PAGE` baseline** (`fcc9fda`'s `is_blank_image()` flagging the shared image fixture, `assert 'FAILED' == 'ANALYZED'`, confirmed identical, extractor change cannot influence technical validation); downstream consumers `test_rule_engine_api`/`test_confidence_api`/`test_normalization_api`/`test_feedback_api`/`test_reports_api`/`test_completeness_api` **98 passed**. Remaining test files reference the two `DocumentType` enums only for routing/constants — zero direct `extract_fields` calls, so unaffected (verified by grep). Nothing committed (user to review).

- **2026-08-18, same-session follow-up: the Employee account now has genuine all-access ("Employee = everything") — a real backend role, not just a frontend view.** The seeded `Verification Officer` account was previously mapped to REVIEWER, so the Validation page showed it "view-only access to this queue" (`ValidationPage` gated `canOperate` on `isOperator`), System Logs 403'd it (backend `require_role(ROLE_IT)`), and the frontend treated *any* non-canonical stored role as a fake "Employee superuser" that the backend would actually 403. Now: `ROLE_EMPLOYEE = "EMPLOYEE"` is a canonical role in `app/auth/roles.py` (`ROLE_GROUPS["Verification Officer"] = ROLE_EMPLOYEE`), and `require_role` in `app/auth/dependencies.py` grants EMPLOYEE every guard by design (single-point superuser check) — so the Employee account is backend-authorized for operator (request documents/reject/submit), reviewer (human review, validation report) and IT (system logs) endpoints. Frontend mirrors it: `frontend/src/utils/roles.js` adds `ROLE_EMPLOYEE`, maps "Verification Officer" → EMPLOYEE, and redefines `isEmployee` from "any non-canonical stored role" to `effectiveRole(user?.role) === ROLE_EMPLOYEE` — an unknown role like "SUPERVISOR" now gets the honest ungated-only nav (dashboard/applications/processing/settings) instead of a fake full view; `ValidationPage.canOperate = isOperator(user) || isEmployee(user)` and `SystemLogsPage.allowed = isIt(user) || isEmployee(user)` open those pages to the Employee account. Deliberately **not** making `isOperator` return true for Employee globally, so `ValidationReportPage.canOpenHumanReview = !isOperator(user)` keeps showing the button to Employee. **Tests**: backend `test_roles.py` updated (four canonical roles, mapping, `require_role` allows Employee for every guard + still 403s a plain REVIEWER on the IT guard) + new `test_employee_account_can_run_operator_actions` in `test_operator_workflow_api.py` (the "Verification Officer" account actually executes a request-documents action end-to-end); frontend `roles.test.js`/`ValidationPage.test.jsx`/`SystemLogsPage.test.jsx`/`Phase1Routes.test.jsx` updated/added (Employee sees operator actions + no view-only note, Employee views system logs, SUPERVISOR restricted). Run results: backend `test_roles.py`+`test_operator_workflow_api.py` 24 passed; `test_system_logs_api`/`test_auth_api`/`test_auth_enforcement`/`test_validation_tasks_api`/`test_human_verification_api`/`test_validation_workflow` 80 passed; frontend full suite 70 passed (12 files); `npm run build` + `npm run lint` clean. The 403-role tests still use canonical `reviewer_client`/`operator_client` so they keep verifying role separation. Also `.gitignore` now covers `worker.heartbeat` (bulk-queue worker liveness file) alongside `ocr_cache`/`.ocr_cache`. Committed with the extractor fix.

- **2026-08-19: `GDA_Abbotabad`'s `ONE_LINK_LETTER` group -- real finding, not a copy-count issue.** Read all 4 cached `GDA_Abbotabad__ONE_LINK_LETTER_copy{1..4}.txt` files in full (1,602 lines total), not just counted. Only `copy1`'s first ~200 lines are genuine 1-Link Application Form content (Form A/1B/Form B). Everything else across all 4 files -- the remaining ~1,000 lines of `copy1`, and the entirety of `copy2`/`copy3`/`copy4` -- is a completely unrelated **Bilateral Agreement/SLA** between KPITB and GDA (17 numbered clauses, full signature blocks) followed by **three separate stamp-paper counterparts of a Participation Memorandum**, each triggering a new "copy" boundary at its own E-stamp/watermark page rather than being recognized as a continuation of the document already open. **This directly confirms, on a real file, the over-fragmentation risk the Bilateral/Tripartite scope-boundary entry above already predicted** ("the more specific stamp-paper boilerplate text... does leave *some* OCR trace... using it as a splitter boundary would risk the same over-fragmentation problem") -- not a new failure mode, the predicted one actually happening. The continuation-grouping fix (`8231d86`) is not misbehaving on its own designed case (an exact repeated strong-title phrase); these boundaries are triggered by something else entirely, which that fix was never meant to catch. `upload/services.py::_ensure_slot_available` would hard-reject a 4th copy against the cap of 3 on a real bulk upload of this file -- but the more important fact is none of the 4 slots correctly represent a real `ONE_LINK_LETTER` boundary at all. Not fixed here -- same "wrong mechanism, needs visual/structural detection, not more text phrases" conclusion as the entry above. **Department decision, 2026-08-19**: the hard-cap rejection this causes is being addressed separately (see the flexible-copy-limit entry below) -- that change stops the crash, it does not touch this absorption bug, which remains open.
- **2026-08-19: `GDC Alpurai Shangla .pdf`'s real split result -- 6 real document types collapsed into one undifferentiated blob, worse than TMA Lal Dir Upper's 5-in-1.** Page-by-page read of all 9 pages (confirmed count, not an earlier stale "15") against the master rules checklist vocabulary. The splitter produced **exactly 1 document** -- a single `OTHER_SUPPORTING_DOCUMENT copy 1` (554 lines) -- but that one label hides: the checklist cover page (page 1, correctly excluded by `68d47d6`), a genuine Account Maintenance Certificate (page 2), a genuine 1-Link Application Form across 3 pages (pages 3-5), a genuine Formal Request Letter (page 6, subject "ON-BOARDING AS A SUB-SELLER WITH KPITB" -- a third real subject-line variant alongside TMA Lal Dir Upper's), an unmapped "Business Certificate" (page 7, headed "ANN-07", doesn't cleanly match any named checklist item -- a new, unresolved ambiguity), and Participation Memorandum/Tripartite content across the final 2 pages (partially unfilled template placeholders mixed with real filled-in fields, a mixed-fill shape not seen elsewhere). **The Authority Letter's absence is confirmed genuine, not part of this absorption bug**: no Authority Letter content exists anywhere in the 9 pages, and the file's own page-1 checklist table corroborates this independently -- every other required item's row is marked "yes"; only the Authority Letter row lacks that mark, meaning the source document itself records it was never submitted. Not fixed -- same absorption mechanism as the entries above, just total rather than partial.
- **[RESOLVED 2026-08-21] 2026-08-19: Zarghuna's bank-account-block structural parser (`origin/feature/zarghuna-bulk-queue`, `16902bd`) -- Tripartite fix initially confirmed correct by direct execution (though a subsequent commit `2039bcd` introduced a `party_1link` regression, which is now fully resolved — see 2026-08-21 entry below); AMC fix initially confirmed NOT to close its own headline bug on a real sample.** Both evaluated with equal rigor: extracted via `git show` (no merge), run directly against real cached text, not just her own docstrings/synthetic fixtures. **Tripartite**: run against `TMA_Lal_Dir_Upper`'s real Participation Memorandum text (pages 8-9) -- all 5 critical fields (`party_1link`, `party_kpitb`, `party_subbiller`, `account_holder`, `account_number`) extract correctly, matching this file's own documented real values exactly (see the Tripartite entry above). Confirmed by execution, clean fix. **AMC**: run against `DG_Sports_KP_Onboarding_Documents__ACCOUNT_MAINTENANCE_CERTIFICATE_copy1.txt` -- the exact file her fix's docstring targets ("all three critical fields empty") -- `_extract_bank_account_block(text)` returns `{}`; the full extractor's critical fields stay empty. **Root cause**: her label matcher anchors same-line value extraction via `.match()`, but this file's real labels carry a parenthetical descriptor suffix her code doesn't strip (e.g. `"Account No. (T-24 System)"`) -- the same-line normalization fails against that suffix and the code never falls through to the next line, where the real value sits. This file also has a 5th real shape not present in any of her 7 new fixtures: three parallel account "generations" (Old/T-24/New-Islamic), each its own Account No./IBAN No. label pair, with `account_holder` stated only in prose ("Peshawar Sports Complex"), never as a labeled field at all. Her 7 new tests (`tests/test_document_analysis_engine.py`) remain formally unrun by pytest -- every attempt in an isolated worktree hits `sqlalchemy.exc.OperationalError: database "finance_verification_test" does not exist`, a missing test-database provisioning gap, not a code defect; not fixed here per the standing rule against touching DB state not understood. Not yet communicated to Zarghuna directly at the time this was found -- since raised with her separately; not this session's to touch (see her PR, still pending on her side). **UPDATE 2026-08-21: CONFIRMED FIXED.** The extractor now correctly handles this real data, extracting the T-24 System account block (`account_holder`/`account_number`/`iban`) successfully. This open decision/gap is fully closed.

- **2026-08-19: AMC 5th-layout gaps fixed (`backend/app/document_analysis/extractors.py`) — the reviewer-confirmed DG_Sports cases: parenthetical system-qualified labels ("Account No. (T-24 System)"), three parallel account "generations" (T-24/CBS/Core), and an account holder stated only in prose. Not committed (user to review).** The DG_Sports AMC OCR text is not on this machine (`docs/ocr_cache/` holds only GDA/TMA files; `Confidential Data/` absent), so the fix was built from the reviewer's structural description with synthetic fixtures (all fake values) and is flagged for real-cache re-verification on the reviewer's copy. Four changes: (1) `_match_label` now strips a parenthetical qualifier that is the *first token* after a field label (`_QUALIFIER_RE`, anchored `\s*\([^()]*\)` so `Account No.: (PK06…)` and `3005642921 (PK06…)` stay values) — the value on the same line after a separator or on the next line is then captured; (2) `_interleaved_scan` now falls through to the bare-label next-line path when an inline value normalizes to nothing, instead of silently dropping the field; (3) `_extract_holder_from_sentence` (new, used only when no labeled holder exists) recovers the holder from ownership-anchored prose ("account of …", "account titled …", "in the name of …", "maintained by …"), with the capture char class case-scoped via `(?-i:…)` so prose words can't leak in, plus N/A/placeholder and lone-parenthetical guards in `_normalize_account_holder`; (4) `_normalize_account_number` tightened — the lenient branch no longer accepts spaces (prose tails were getting captured), and a last-resort run-token scan extracts a structurally valid IBAN/digit token from a prose-laden remainder. Two real-data results confirmed against the local cache (values masked): the previously-dropped `TMA_Khal_Dir_Lower__ONE_LINK_LETTER_copy3` "account number (1BAN) PK… titled as Tehsil General Account" line now yields the clean IBAN token (pre-change: absent; interim bug caught in-session: whole prose sentence as account_number), and all 4 GDA AMC copies + the Tripartite sample extract identically to before. **Also caught and fixed in-session: a column-block misdetection** — a plain label/value layout whose value line looked like a caption header was being read as a stacked table (landing an all-digit value in the account_holder slot and the IBAN line in the account slot); `_extract_column_block` now rejects a block unless *every* recognized value slot normalizes to something real. **Tests**: 6 new synthetic cases in `test_document_analysis_engine.py` (parenthetical label value-on-next-line, parenthetical label inline-after-colon, parenthetical IBAN not eaten, first-of-parallel-generations, sentence-only holder + no false positive on "account is maintained with us", OCR-noise prose tail → clean token) — **71 passed**; `test_document_analysis_api.py` still exactly the documented 13 `TECH_BLANK_PAGE` / `assert 'FAILED' == 'ANALYZED'` baseline, zero new failures. **Also resolved: the "finance_verification_test DB provisioning" concern is a non-issue** — `tests/conftest.py` already auto-creates the `*_test` database if missing (`_create_test_database_if_missing`, requires CREATEDB on the role) and applies `alembic upgrade head` before the suite (CI's 815-test run proves it works in CI too); it only raises clear instructions if the role lacks CREATEDB. Nothing committed.

- **2026-08-20: System Logs UI replaced with two IT-only business pages — Application History (`/application-history`) + Performance (`/performance`) — backend + frontend + tests, not committed (user to review).** Scope per user decision: remove System Logs from the frontend UI only (sidebar, dashboard quick actions, routes, Settings); the backend `system_logs` module, the `audit_logs` table, every audit write site and `test_system_logs_api.py` are untouched (the audit infrastructure stays; only its one UI surface is gone). Backend work on branch `feature/zarghuna-bulk-queue`:
  - **Event-semantics verification first, before writing code**: `applications.submitted_at` = creation time; `DOCUMENTS_REQUESTED` is written by `operator_workflow.services.request_documents` (has created_at/reason/missing_document_types/actor); **`upload.services.py::_record_documents_received` only writes `DOCUMENTS_RECEIVED` when status is `NEEDS_DOCUMENTS` at call time** — the bulk path flips to `PROCESSING` first (lines 277-280) so bulk uploads on `NEEDS_DOCUMENTS` apps record no received event; the single-upload path records it (line 184) before any status change. Final decisions live on `human_reviews` (reviewer_name/reviewed_at/decision/comments/rejection_reason), NOT on validation_history (`REVIEW_*` ValidationEventType values exist in the enum but are never written). `APPLICATION_PIPELINE` job `completed_at` marks when `PENDING_REVIEW` was set.
  - **New `backend/app/application_history/`**: `GET /applications/history` (list: metadata + most-recent-workflow-event per app via a bulk 3-query latest-event pick across history/reviews/documents — no N+1; query search on id/name/creator; status filter; offset/limit) and `GET /applications/{application_id}/timeline` (full chronological lifecycle: creation, uploads, workflow events, pipeline completion, review decisions — business-facing labels/detail assembled server-side, never raw JSON/PII). IT-only (`require_role(ROLE_IT)`, Employee inherits), 404 for unknown application.
  - **New `backend/app/performance/`**: `GET /performance/overview` (totals, status counts, avg waiting/processing/review/turnaround — averages computed only over applications that actually have the metric, so all-in-flight sets report `null` not a misleading "0 days"; total resubmissions + missing-document cycles) and `GET /performance/applications` (per-app `waiting_seconds`/`processing_seconds`/`review_seconds`/`total_turnaround_seconds` + `resubmissions` + `missing_document_cycles` + **evidence `TimeSpan`s behind every number**: label/start/end/duration_seconds/open/detail). **Timing model (user-mandated)**: waiting = each `DOCUMENTS_REQUESTED` paired with the next `DOCUMENTS_RECEIVED`; an unmatched request is an **open span** (start → now, `open=True`, `end`/`duration_seconds` null) — shown as evidence but never counted; internal processing = each queue job's started_at→completed_at (→now while PROCESSING); review = pipeline completion → latest review decision (→now while PENDING_REVIEW); total turnaround = submitted_at → latest decision (→now in flight). IT-only.
  - **Repository additions**: `queue_job_repository.list_all_by_application()` (all job types incl. APPLICATION_PIPELINE); `application_repository.search()` (query on id/name/created_by + status filter + pagination) — return annotation is `tuple[Sequence[Application], int]`, because the class's existing `list` method shadows the builtin `list` in class-scope annotations (`tuple[list[...]]` raised `TypeError: 'function' object is not subscriptable`).
  - **Route ordering is load-bearing**: `application_history_router` is registered in `api/__init__.py` BEFORE `upload_router` so the literal `/applications/history` path wins over `/applications/{application_id}`. OpenAPI verified: 72 paths incl. all four new endpoints.
  - **Tests**: `tests/test_application_history_api.py` + `tests/test_performance_api.py` (role guards incl. Employee-inherits and non-IT 403, empty overview nulls, averages math, per-app evidence spans, open unmatched-request span, search/pagination, timeline merge+ordering, 404). **53 passed** for the two new files + `test_system_logs_api` + `test_operator_workflow_api` + `test_auth_enforcement` (30 regression + 23 new). Fixed mid-test: routes `application.application_id` → `application.id` (ORM object, no alias); `test_history_search_by_creator` sets `created_by` via repository because the API derives the actor from `current_user.name` and ignores any payload. The `PydanticJsonSchemaWarning "Default value Ellipsis is not JSON serializable"` is pre-existing (verified via `git stash` — present without the changes).
- **Frontend (same session)**: deleted `pages/SystemLogs/` (page + CSS), `hooks/useSystemLogs.js`, `services/systemLogs.js`, `SystemLogsPage.test.jsx`; removed the `settings/system-logs` route/filter and the System Logs admin link from Settings; replaced the dashboard `system-logs` quick action with `application-history` + `performance` (Employee and IT sets); added IT-only top-level sidebar items in the System section (`History`/`BarChart3` icons). Two new pages built entirely from the real backend data above, business-facing only, IT-gated in the UI with the same access-denied pattern System Logs used: **Application History** (search/status filter, per-app row with status badge + last-event chip, expandable chronological timeline panel with kind icons/actor/detail) and **Performance** (summary cards incl. avg durations via a new `formatDuration` helper in `utils/format.js` + "No completed spans yet" honesty for null averages, per-app table with waiting/processing/review/turnaround columns, expandable evidence spans per row with `Open` tags and "→ now" ranges for unmatched requests). Frontend suite **90 passed (17 files)**; `npm run build` clean; `npm run lint` 0 errors (same 4 pre-existing fast-refresh warnings). Nothing committed — user to review. Note: the `test_document_analysis_api.py` 13-failure `TECH_BLANK_PAGE` baseline is unchanged and unrelated to this work.

- **2026-08-20, Phase 2 checkpoint: Application History cycle grouping COMPLETE.** Frontend `ApplicationHistoryPage.jsx` now groups `DOCUMENTS_REQUESTED → DOCUMENTS_RECEIVED` pairs into visual resubmission cycles. Implementation: purely frontend, no backend/schema/role changes; uses only backend workflow events; no timestamp heuristics; preserves chronological ordering and underlying events; supports closed cycles, open/unmatched requests, and multiple cycles; preserves existing document-upload grouping and structured metadata. Files changed: `ApplicationHistoryPage.jsx`, `ApplicationHistoryPage.module.css`, `ApplicationHistoryPage.test.jsx`. **Verification**: Application History tests **12 passed**; full frontend suite **97 passed (17 files)**; ESLint **0 errors** (4 pre-existing fast-refresh warnings); production build **successful**. Access model unchanged: Employee=all, IT=allowed, Operator=denied, Reviewer=denied. UX is intentionally not final — current cycle wording is technical ("Resubmission Cycle 1"), future should be business-oriented ("Documents requested — Cycle 1 of 2"). Full checkpoint at `docs/PHASE2_CHECKPOINT.md`. **Next step: real-data UX validation** before Phase 3. Do NOT implement: UX wording changes, new cycle API, Performance→Application History drill-down, new database tables, new backend cycle calculations, or additional cycle heuristics — those belong after validation. No commit/push unless explicitly instructed; no backend changes during validation unless an actual API deficiency is discovered.

- **2026-08-21: `feature/zarghuna-bulk-queue` merged into `main` (23 commits).** Shipped: roles (Employee/Operator/Reviewer/IT with real role guards, not just UI), AMC/Tripartite structural bank-account-block parser, performance metrics API+page, application history timeline, resubmission workflow fix, remember-me auth sessions + ENVIRONMENT fail-closed.
  - **AMC extractor gap resolved**: previously "returns {} on DG_Sports real data" (see 2026-08-19 entry), now **CONFIRMED FIXED** by direct re-execution against the same real file — correctly extracts the T-24 System account block (`account_holder`/`account_number`/`iban`) from the 3-account-generation structure.
  - **Test accounts now real and merged**: operator/reviewer/it, password `12345678`, plus the existing all-access Employee account.
  - **Full suite**: 912 passed, 12 failed (same pre-existing `fcc9fda` `TECH_BLANK_PAGE` baseline, root cause unchanged), 1 unrelated environment collision (`test_health.py` vs live `worker.heartbeat`, confirmed passes clean with worker stopped, not a real bug).

- **2026-08-21: `AuthorityLetterExtractor` `organization_name` garbage-capture FIXED (commit `6759c21`).** Root cause: the primary `organization_name` regex terminated on `\n`, so a line ending `"...on behalf of the\n"` captured the bare word `"the"` instead of the real organization. **Two documented real cases fixed**: `TMA_Lal_Qilla_Dir_Lower` and `TMA_Samarbagh_Dir_Lower` (both OCR'd and verified directly; Samarbagh was OCR'd fresh this session). Fix: tightened the regex to require a non-whitespace character before the newline, and added `"the"` to `_GENERIC_ORG_REFERENCE` so any remaining bare-pronoun captures trigger the letterhead fallback. Full 9-sample `AUTHORITY_LETTER` sweep: all other files byte-for-byte unchanged. Full `test_document_analysis_engine.py` + `test_reports_api.py` suite: **89/89 passed**.

- **2026-08-21: `BLUR_THRESHOLD` false-positives FIXED (commit `5e20a81`).** The global `variance_of_laplacian` score was giving false positives (flagging as blurry) on sparse, mostly-whitespace pages (like synthetic tests) because the zero-edge background diluted the variance. A tiled `max()` approach was tested but found to create a severe false-negative risk: a completely blurry page with a single sharp artifact (like a stray staple mark) scored `1403.94` and incorrectly passed the `100.0` threshold. Fix: implemented a tiled **95th-percentile** variance calculation. Validated: a real blurry document scores `18.34` (correctly fails), the stray-mark stress case scores `26.88` (correctly fails), and a full sweep of all 9 real PDF types scores safely between `1549` and `8678` (correctly passing with no regression). 17/17 technical validation tests pass.

- **2026-08-21: `party_1link` regression FIXED in `TripartiteAgreementExtractor` (commit `6c26f8d`).** The structural block parser merge (commit `2039bcd`) changed extraction order such that regexes ran first and their results were kept, causing `party_1link` to eagerly capture the Authority Letter preamble text (`"1-Link and the Khyber Pakhtunkhwa Information Technology Board (KPTIB) on behalf of TMA Thall."`) at the top of compound files like `TMA_Thall_Agreement`. Fix: added a fixed-width negative lookbehind `(?<!related to )` to the `party_1link` pattern to exclude the Authority Letter boilerplate phrase `"matters related to 1-Link"`. Validated against real data: `TMA_Thall_Agreement` correctly skips the Authority Letter and extracts `"1LINK (Private) Limited..."` from the actual Participation Memorandum section. `party_kpitb` is still correct, and there is no regression on `TMA_Lal_Dir_Upper` ONE_LINK_LETTER copies. Note honestly: the extracted `party_1link` value still isn't fully clean (it trails on with `"...and Khyber Pakhtunkhwa..."` because it lacks a comma to stop it) — this is a pre-existing imperfection returning to its pre-regression baseline, not a newly introduced issue. Tests: 79+10 passed. This fully resolves the in-session finding that `party_1link` was producing "WORSE / different garbage" after the merge.

- **2026-08-21: Splitter absorption instance on Samarbagh diagnosed (not fixed).** While OCR'ing `TMA_Samarbagh_Dir_Lower.pdf`, page 8 was absorbed into the open `BUSINESS_REQUIREMENT_DOCUMENT`. Diagnosed the cached text: page 8 is a Formal Request Letter containing the phrase "APPLICATION FOR ONBOARDING ON 1LINK 1BILL SERVICES". This caused a weak match for `ONE_LINK_LETTER`, but without hitting the strong threshold, it defaulted to being absorbed as a continuation. This confirms it is the exact same root mechanism as the `GDA_Abbotabad` and `GDC Alpurai Shangla` cases, not a new failure mode. Left unfixed, as it requires a different detection mechanism entirely.

- **2026-08-21: Tripartite Agreement compound-splitting scope assessment.** With the `party_1link` regression fixed above, the broader problem remains: relying on text patterns to split complex compound documents (Authority Letter + Participation Memorandum + SLA) is fundamentally fragile. Four options were scoped for future work (none committed to now): (1) Structural NLP block parsing (fragile to OCR noise, fails Section 8 visual requirements), (2) Visual/CV image classification (solves Section 8 visually but requires training a new ML model), (3) Layout-aware object detection like YOLO (finds physical stamps/signatures, requires ML training), (4) Multi-modal LLMs (highly accurate without training, but introduces high latency, API cost, and privacy concerns). Logged as scoping notes for a future decision.

*(The entries below are from `audit/backend-verification-2026-08-20`, a parallel investigation branch that diverged from `main` at `d1668f0` and is being merged in on 2026-08-22 -- see the dated merge-resolution entry appended at the end of this section for how the two independent timelines were reconciled. Its own 2026-08-19 entry on Zarghuna's bank-account-block parser is dropped here as a pure duplicate: it is essentially the same finding as the [RESOLVED 2026-08-21] entry above, minus that entry's later "CONFIRMED FIXED" update.)*
- **2026-08-20: TripartiteAgreementExtractor confirmed structurally not-production-ready against a second real sample, `TMA Thall Agreement.docx` -- and this is not a regex-tunable problem.** Distinct from `TMA_Lal_Dir_Upper`'s Participation Memorandum sample above (3/6 correct, 2/6 garbage, 1/6 honest miss). This `.docx` was cached whole (`TMA_Thall_Agreement__TRIPARTITE_AGREEMENT_copy1.txt`, 301 lines) -- it was never run through `DocumentSplitter.split_bulk_pdf`, which only handles PDF bytes, so it bundles an Authority Letter, a Participation Memorandum, the actual Agreement, a Formal-Request-style letter, and a signature table all in one file. Isolating just the "AGREEMENT FOR DIGITAL PAYMENT COLLECTION VIA PAYMIR" section and re-running the extractor: `party_kpitb` extracts correctly (KPITB is named directly); `party_1link` and `party_subbiller` land on garbage/empty because this real document has no formal "hereinafter referred to as 1-Link" naming clause anywhere; `account_holder`/`account_number` also fail because the real account clause is narrative prose ("...deposited in PK82KHYB50130987142561901...titled as TEHSIL GENERAL ACCOUNT TMA THALL"), not a labeled `Account Title:`/`Account Number:` line the patterns require. No pattern changes attempted -- per the CrossPeriodRule/CrossBranchCodeRule caution already on record above, tuning regex to fit one atypical real sample without generalizing repeats a documented mistake. Also worth flagging: `f8edde7` ("tighten TripartiteAgreementExtractor party/account patterns") was committed before this sample existed, so its correctness against real Tripartite content remains unverified either way -- not confirmed right, not confirmed wrong.
- **2026-08-20: 2 new `organization_name` garbage-capture cases found in `AuthorityLetterExtractor`, distinct from the honorific bug already fixed in `ccaccff`.** Real samples `TMA_Lal_Qilla_Dir_Lower__AUTHORITY_LETTER_copy1.txt` and `TMA_Samarbagh_Dir_Lower__AUTHORITY_LETTER_copy1.txt` both extract `organization_name: 'the'`. Source text: "...to deal with and conduct correspondence and matters related to 1-Link and the\nKhyber Pakhtunkhwa Information Technology Board (KPITB) on behalf of the\nAuthority." -- the pattern's capture stops at the line break before reaching KPITB's actual name, landing on the dangling "the". Vague/awkwardly-wrapped source prose, not a regression from `ccaccff`'s fix (different field/case). Note for whoever picks this up: `feature/afsana-validation-logs` commit `89b4d60` (2026-08-19, unmerged) generalizes a *different* `AuthorityLetterExtractor` field (`focal_person_name`, for a third department, `TMA Lal Dir Upper`) -- it does not touch `organization_name` and would not fix this case if merged as-is. Separately, 1 of the 8 real samples checked (`TMA_Thall_Hangu__AUTHORITY_LETTER_copy1.txt`) is not an Authority Letter at all -- it's a "MASTER CHECKLIST" cover page listing "Authority Letter" as a checklist row item, misclassified by the splitter. Zero-field extraction there is the correct outcome for that page's actual content; if there's a bug, it's in splitter classification, not this extractor. Neither issue fixed here.
- **2026-08-20: `BusinessRequirementDocumentExtractor` -- header-echo capture pattern, fixed 2026-08-21.** Across real cached samples: some captures echo a section header verbatim as the field value (e.g. a field capturing the literal string `'SERVICES OFFERED'` instead of the services actually listed beneath it) -- the capture group doesn't skip past the header line into its content. (Resolved via 7-line window heuristic, see below).
- **2026-08-20: root cause confirmed for the 2 zero-field BRD samples (`TMA_Lal_Qilla_Dir_Lower__BUSINESS_REQUIREMENT_DOCUMENT_copy2.txt`, `TMA_Samarbagh_Dir_Lower__..._copy2.txt`) -- two independent real bugs stacked in the same sample.** Read both raw cached files in full, split at their `--- page break ---` marker, and re-ran the extractor against each half in isolation to separate the two mechanisms rather than inferring from the combined zero-field result alone. **(1) OPEN BUG - Splitter continuation-absorption, same mechanism already documented for `TMA_Lal_Dir_Upper`/`GDC_Alpurai_Shangla` above**: each `copy2` file is actually two unrelated real documents merged into one -- a genuine BRD continuation page followed by a completely separate onboarding-request letter to KPITB's Director (\"Subject: APPLICATION FOR ONBOARDING...\"). `app/preprocessing/splitter.py`'s `FORMAL_REQUEST_LETTER` entry has no phrase matching this real subject line, so the page is silently absorbed as a continuation of the still-open BRD group. **(2) FIXED - `BusinessRequirementDocumentExtractor` gap on real content**: The `digitization_intent_confirmed` pattern was broadened to match the full department name. Verified against the 5 real cached samples present at the time via venv interpreter (TMA_Lal_Qilla copy1/copy2, GDA_Abbotabad, DG_Sports, TMA_Khal). *Correction (2026-08-21): An earlier commit (bd83c22) and entry claimed this was verified against "all 7 real samples", but TMA_Samarbagh copy1/copy2 had actually been deleted from the cache for testing and were missing during that sweep. The fix itself was safe because Samarbagh shares the exact Lal Qilla template vocabulary, but the physical files were not present.* **(3) FIXED (2026-08-21) - `revenue_services_listed` header-echo**: The regex was updated from a blind `.extract()` on the anchor to a line-aware boundary pattern `[^\n]*(?:\n[^\n]+){0,7}` that captures the header plus up to 7 subsequent complete lines. **Warning: This is a fitted heuristic tuned to the observed samples, not a general semantic boundary.** It will pull in adjacent, unrelated content if the real list is shorter than 7 lines (e.g., bleeding into digitization-intent text in synthetic fixtures), and will truncate real lists longer than 7 lines. Acceptable risk because this is a non-critical human-review context field, not a gating field, but must be understood as a heuristic, not a clean general fix.
- **2026-08-20: DB-guard investigation -- inconclusive, no live reproduction achieved.** The 68 failed / 122 errors IntegrityError-heavy test run earlier today prompted an extended investigation into whether `tests/conftest.py`'s DATABASE_URL-rewrite-to-`_test` guard was misfiring. No live reproduction of the guard firing was achieved during this investigation -- every reproduction attempt instead hit either an invocation-method quirk (`cmd /c` vs direct PowerShell interpreter invocation producing inconsistent results) or an unrelated, since-resolved false alarm (a `ModuleNotFoundError` for psycopg that could not be reproduced when the interpreter was invoked directly and consistently). The rewrite logic (lines 33--39 of `backend/tests/conftest.py`) and the guard in `_wipe_database()` were both read and confirmed present and unchanged since commit `d1668f0`. Left genuinely unresolved: whether the original 68-failed/122-error run was a real guard misfire or was actually hitting substantive test failures whose error signature was misread as IntegrityError-driven DB contamination. **If this resurfaces**: before investigating `conftest.py` again, first verify `$env:DATABASE_URL` is unset in the invoking shell (`echo $env:DATABASE_URL` in PowerShell -- must be empty), and always invoke pytest via `& '<path>\python.exe' -m pytest ...` directly rather than through `cmd /c ... && python.exe -m pytest ...`, since mixed invocation methods produced several misleading one-off errors this session.

- **2026-08-22: `audit/backend-verification-2026-08-20` merged into `main` (commit `904ab84`), closing the timeline reconciliation promised in the bridging note above.** The two branches had independently fixed the same `AuthorityLetterExtractor.organization_name` garbage-capture bug (main's `6759c21` vs the audit branch's `0be3ff6`) -- both variants were run against all 9 real cached Authority Letter samples before choosing, not picked by "ours wins": byte-identical results on every sample, so the choice came down to which was structurally safer, and the audit branch's bounded one-newline-tolerance variant was adopted, matching the surviving class docstring's own documented convention. `.gitignore` additions from both branches were combined (no conflict, pure union). The `CONTEXT.md` timelines were combined by hand, dropping the audit branch's own 2026-08-19 entry on Zarghuna's bank-account-block parser as a pure duplicate of the `[RESOLVED 2026-08-21]` entry already on `main` (same finding, minus that entry's later "CONFIRMED FIXED" update). The merge also brought in the audit branch's `SESSION_HANDOFF_2026-08-20.md`, its BRD fixes, its DB-guard investigation notes (both entries above), and additional test coverage. **Post-merge verification, not just an assertion**: `test_document_analysis_engine.py` went from 60 to 79 tests (confirmed via actual diff of the merged file, not inferred from the audit branch's own claim) -- audit branch contributed 0 new `def test_` functions of its own (its 3 changes were assertion updates to pre-existing tests), main's own prior session work independently added the other 19; `test_reports_api.py` 10 passed; full suite 912 passed / 13 failed (known baseline, unchanged). A separate stale-assertion regression surfaced immediately after (`revenue_services_listed` in `test_document_analysis_api.py`, one of the merge's pre-existing tests never updated when an unrelated BRD header-echo fix landed) -- fixed same day as its own commit (`4f8c390`), suite confirmed back to the exact 13/912 baseline. `audit/backend-verification-2026-08-20` was confirmed fully merged (`git branch --merged main`) and deleted, both local and remote, after this verification.
- **2026-08-22: `BilateralAgreementExtractor` rewritten against real content, first real-sample validation for this type (commit `fc27a24`) -- and it surfaced a downstream rule-engine bug, fixed the same day (commit `19a2e29`).** The original patterns were written blind against `docs/Master_Rules_Combined.md` and matched none of the real content on either of the two real samples now on file (Conservator Wildlife Peshawar Zoo, a second KP department) -- wrong platform terminology (spec's "Digital Muhasil/PayMin/Paymere BCX" vs the real documents' own defined "PAYMIR"), wrong section anchor ("Section 5.2" vs the real bare "5.2."), and the same table row-index garbage-capture bug (`account_number: '01'`) already fixed elsewhere for AMC/Tripartite. Rewritten field-by-field against both real samples, reusing the existing `_extract_bank_account_block` structural parser for the account fields (see `extractors.py`'s `BilateralAgreementExtractor` docstring for the full before/after). One real n=2 generalization bug caught during validation: the `transaction_charges` stop boundary matched a false numbered list inside an unrelated e-stamp verification page physically inserted in one real sample between the charges table and the next section -- fixed with two stop conditions (an all-caps section header, or the OCR cache's own page-break marker). **Result, confirmed on both real samples**: 6 of 7 fields correct; `account_holder` is a genuine structural honest-miss on both -- neither real sample's bank-account table has an "Account Title"/"Account Holder" column at all, only Bank Name + Account No.
  That honest-miss finding then broke `CrossAccountHolderRule` (`rule_engine/rules/cross_document_rules.py`), which required `BILATERAL_AGREEMENT` as a participant on `account_holder` -- the same shape of problem as the already-documented `CrossBranchCodeRule`/`CrossPeriodRule` cautionary tales, just discovered for a rule that was actually registered and passing (on stale, unrealistic test fixtures) rather than one already known to be broken. Fixed by narrowing `CrossAccountHolderRule.participants` to `ACCOUNT_MAINTENANCE_CERTIFICATE`+`TRIPARTITE_AGREEMENT` only (both genuinely extract `account_holder` for real) -- `BILATERAL_AGREEMENT` removed, not the whole rule unregistered, since the AMC↔Tripartite comparison remains real and meaningful. Propagating this through the test suite (`test_rule_engine_api.py`'s `BILATERAL_STATEMENT_TEXT`, `test_reports_api.py`'s two hardcoded fleet-wide `overall_confidence` assertions, `test_rule_engine_rules.py`'s generic cross-document unit test) took one more commit (`c56efee`) to fully settle; full suite confirmed back to the documented 13-failed baseline (913 passed, +1 over the prior 912 from a new test this work added) after all four commits landed.
- **2026-08-22: `TripartiteAgreementExtractor.branch_code` real-sample validation attempted and blocked -- accepted, not blocking, needs future work.** Proposed fix: port `OneLinkLetterExtractor`'s old parenthetical branch-code pattern (`r"branch\s*\([^()]*\((\d{3,6})\)+"`, matches a "Branch (0312)"-shaped clause) into `TripartiteAgreementExtractor`, whose current pattern (`r"(?:Branch Code|Branch)\s*:\s*(.+)"`) requires a literal colon and never matches that shape at all. That pattern was itself removed from `OneLinkLetterExtractor` in `26d57eb` (2026-08-19, department decision) for the same reason `CrossBranchCodeRule`/`CrossPeriodRule` were unregistered: one real clause shape is an unambiguous single-account sentence, but the other real shape is a multi-bank reference table with no textual indication of which entry is operative, and the pattern can't tell the two apart.
  Why it's not fixable today: the only real Tripartite Agreement sample on file (`TMA_Thall_Agreement__TRIPARTITE_AGREEMENT_copy1.txt`, already flagged 2026-08-20 above as structurally not production-ready) has no branch code anywhere to validate against, filled or otherwise. Its prose clause reads `"...maintained with (BANK OF KHYBER THALL) in its branch (Branch name/address);"` -- a literal unfilled template placeholder, not real data. Its account table (`"01 | Bank of Khyber Thall Branch | PK82KHYB..."`) has no separate branch-code column either -- "Branch" is just part of the bank's name. The real `"Branch (0312)"` text that motivated this fix exists only in two other real files (`TMA_Khal_Dir_Lower`, `TMA_Lal_Dir_Upper`), and in both cases it was cached and classified as **ONE_LINK_LETTER**, not Tripartite -- neither source PDF has any TRIPARTITE_AGREEMENT-classified copy at all. There is currently no way to real-sample-validate a Tripartite-specific branch_code pattern; porting a pattern that only worked on a different document type would repeat the exact mistake `CrossBranchCodeRule`/`CrossPeriodRule` already document.
  What a real fix requires: a genuine real Tripartite Agreement sample that actually states a branch code (filled, not a placeholder) to validate against. Until one exists, `branch_code` stays as-is (a colon-anchored pattern that honestly misses on the one real sample checked, not garbage) -- left alone rather than swapped for an equally-unvalidated alternative.

- **2026-08-22: `feature/afsana-validation-logs`' remaining 3 extraction commits reconciled -- 2 small pieces landed, the rest dropped, one drop caught by real-sample re-validation, not by inspection alone.** `09e099a`/`685ec5d`'s `BilateralAgreementExtractor` prose-pattern changes were validated (per their own commit messages) against `GDC_Madyan_Swat`'s real "Participation Memorandum" text -- **that text is real, but the splitter classifies it as `ONE_LINK_LETTER`, not `BILATERAL_AGREEMENT`**, confirmed by actually OCR'ing and splitting the source PDF today (it was never in the cache before): 5 documents produced (1 `ACCOUNT_MAINTENANCE_CERTIFICATE`, 3 `ONE_LINK_LETTER` copies, 1 `OTHER_SUPPORTING_DOCUMENT`), zero `BILATERAL_AGREEMENT`. `BilateralAgreementExtractor` never runs on this content in the real pipeline -- the branch's validation fed the text directly into `extract_fields(text, BILATERAL_AGREEMENT)` in a test, bypassing real document-type routing entirely. Dropped, not merged: today's from-scratch rewrite (`fc27a24`, see above) is validated against the two real samples that actually do route to this extractor. `ac5914e`'s `AccountMaintenanceCertificateExtractor` next-line/parenthetical/combined-label regex changes are superseded outright -- confirmed by running her own real-sample fixture (`GDC_Madyan_Swat`'s genuine AMC page, now cached) directly against current main: `_extract_bank_account_block` (the shared structural parser that replaced AMC's regex-based account fields entirely after she diverged) already gets `account_holder`/`account_number`/`iban`/`branch_name` right, without her patch. Her one still-relevant piece -- adding a bare `"Date"` label alternative to `issue_date`, since Madyan Swat's real AMC only has an unlabeled `"Date: 17/02/2026"` line -- is a real, still-open gap, but the fix itself is unsafe: tested against all 6 other real AMC samples on file, it fixes 2 genuine honest-misses and introduces 2 new garbage captures, one a clear regression (`GDA_Abbotabad copy4`: `"specific"` -> `"d"`, a single meaningless character). Not landed -- fixing 2 samples by breaking 2 others is not a net improvement. **Landed instead, both real and independent of the above**: `frontend/src/data/documents.js` gained `OTHER_SUPPORTING_DOCUMENT`/`BULK_UPLOAD` catalogue entries (`6c794c9`) -- real backend `DocumentType` enum values the frontend catalogue was missing, previously silently mislabeled as "Authority Letter"; `_parse_date` gained hyphenated date format support (`%d-%m-%Y`, `%d-%b-%Y`, `%d-%B-%Y`, `e2ce416`) -- purely additive, though its cited real-sample motivation lives in the same misrouted `ONE_LINK_LETTER` content and no currently-parsed real field was found anywhere in the cache that actually needs it yet; kept anyway since it can't regress anything already working. `25c8508` (the focus-trap fix) was already cherry-picked earlier today. With this, every commit unique to `feature/afsana-validation-logs` has been accounted for -- see the branch-reconciliation-status entry immediately following this one.

- **2026-08-22: `feature/afsana-validation-logs` fully reconciled -- nothing left un-accounted-for, branch deletion still not done here.** Confirmed by diffing the full tree at the branch's merge-base against its tip (not just re-reading `git log main..branch`, which will always show these commits as "unmerged" by SHA -- cherry-picking and manual patches create new commits, they don't make the old ones ancestors of `main`): every file the branch touches beyond that merge-base -- `backend/app/core/config.py`, `SessionTimeoutModal.jsx`, `ConfirmDialog.jsx` (all from `25c8508`, cherry-picked earlier today as `9c28012`), `backend/app/document_analysis/extractors.py` + `backend/tests/test_document_analysis_engine.py` (the 3 extraction commits, reconciled above), `frontend/src/data/documents.js` (`09e099a`'s catalogue fix, landed) -- is accounted for. The branch's own `CONTEXT.md` edits were not merged verbatim; main's independent, more current narrative already covers the same ground. Local branch ref (`feature/afsana-validation-logs`) was stale throughout this reconciliation -- it pointed at an old commit already an ancestor of `main`; all comparisons here used `origin/feature/afsana-validation-logs`. Deleting the branch (local and remote) is a deliberate action, not something to do as a side effect of a docs update -- left for explicit confirmation.

- **2026-08-22: `SCHEDULE_OF_CHARGES` removed from the required checklist -- was silently making `APPROVED` unreachable for every real application, not just an extraction gap (commit `6cd94ee`).** Abdul's decision: it is not a real standalone document in this checklist -- its content (the Payment Methods & Charges clause) lives inside the Bilateral Agreement, confirmed today when both candidate real Schedule of Charges files turned out to actually be Bilateral Agreements. Investigated before any code change: `DocumentScheduleRule` (`DOC_SCHEDULE_OF_CHARGES_PRESENT`) was registered and hard-FAILed (severity ERROR) whenever the type was absent, and per `OVERALL_STATUS_PRECEDENCE` any FAIL forces the whole application's `overall_status` to FAIL. Since **zero real samples across every real source file processed this entire session (~21 files) have ever been splitter-classified as `SCHEDULE_OF_CHARGES`** -- confirmed by re-checking the full OCR cache, not assumed from the 2026-08-17 blocker entry alone -- this rule was an unconditional, permanently-unsatisfiable blocker: no application, however complete otherwise, could ever have reached `APPROVED` while it stayed registered. This is a materially bigger finding than a missing extractor; it's the same failure shape as `CrossBranchCodeRule`/`CrossPeriodRule` but for a top-level required-document-presence rule rather than a secondary cross-document check, and it had gone unnoticed as "the real, universal blocker" until traced through explicitly today (the existing application-`11189` live-demo entry already showed the empirical `FAILED` result, but hadn't been connected to "this specific rule structurally cannot ever pass").
  Cross-checked, not assumed: Master Rules §9's content requirements (charge/transaction information, PKR currency, completeness) are near-verbatim the same substance already required by §7's own "Section 5 — Transaction Charges" clause, and `BilateralAgreementExtractor.transaction_charges` (rewritten today, see above) already captures that full PKR fee table on both real Bilateral samples. `BILATERAL_AGREEMENT` is already in both `SIGNATURE_DOCUMENT_TYPES` and `STAMP_DOCUMENT_TYPES` (`rule_engine/constants.py`), covering §9's signature/stamp requirement too. No real extraction gap results from treating the two as equivalent.
  Removed from both `completeness/constants.py` and `rule_engine/constants.py`'s `REQUIRED_DOCUMENT_TYPES`. `DocumentScheduleRule` unregistered, not deleted (matches the `CrossBranchCodeRule`/`CrossPeriodRule` precedent — the class stays defined in `rule_engine/rules/document_rules.py` for if a genuine sample ever surfaces). `upload/constants.py`'s 6-copy `MAX_COPIES_BY_DOCUMENT_TYPE` entry dropped (falls back to the default 1-copy limit); its `DOCUMENT_TYPE_SLUGS` entry deliberately kept (a plain dict lookup with no default, used for storage regardless of required-checklist status — removing it would crash storage if the splitter's still-in-place title-match phrases ever did fire on a real page). `preprocessing/splitter.py`'s phrase table left untouched — harmless if it never matches, removing it would be unrelated churn.
  **Important, explicitly checked before claiming success**: this does not by itself unblock any specific already-known-incomplete application. Application `11189` (`TMA Khal Dir Lower.pdf`) was missing 5 of 8 required types; removing this one still leaves it missing `FORMAL_REQUEST_LETTER`/`ACCOUNT_MAINTENANCE_CERTIFICATE`/`BILATERAL_AGREEMENT`/`TRIPARTITE_AGREEMENT`, so it still fails `overall_status`. What changed is that `APPROVED` is now *reachable in principle* for an application that genuinely has all 7 of the remaining required types — before today, it was not reachable for any application at all.
  6 test files had hardcoded assumptions on the old 8-required-type/53-rule counts, fixed: `test_rule_engine_rules.py`, `test_reports_api.py`, `test_rule_engine_api.py`, `test_completeness_api.py`, `test_human_verification_api.py`, `test_operator_workflow_api.py` — plus two fleet-wide `overall_confidence` constants recalibrated (`0.9701` → `0.9587`, same established recalibration convention as earlier today). Full backend suite: 52 rules now (was 53), 912 passed / 13 failed (was 913/13) — same 13 pre-existing named failures, the 1-test drop is the removed `SCHEDULE_OF_CHARGES` parametrize case, not a regression. Frontend suite: 97 passed, 0 failed.

## What's actually left to build

1. **Extraction support for the rule-engine gaps above**, if those checks are wanted — this is the real remaining scope, not UI work.
2. ~~Field-level review widget for the `validation` module~~ — **resolved 2026-08-19: this was stale, and directly contradicted the finding above (line 133: the `validation` module "is vestigial, not awaiting integration," reconciling it with `human_verification` is "a product decision... not a code fix — do not wire this module up reactively").** The old wording here treated wiring `POST /validation/fields/{id}/verify|correct` into a UI as legitimate open work, which is exactly what line 133 says not to do reactively. Investigated before building anything: the equivalent feature already exists, complete and live, on the module actually used in production — `human_verification`'s `ReviewFields.jsx`/`HumanReviewPage.jsx` already lets a reviewer correct any extracted field inline (value + optional reason), submitted as part of the final APPROVE/CORRECT/REJECT decision. The real gap was test coverage, not a widget: that component tree had zero frontend tests before tonight. Closed — 17 new tests added (`ReviewFields.test.jsx`, `HumanReviewPage.test.jsx`; commit `dedb606`), full frontend suite 31 passed/0 failed. Nothing remains open here. Do not resurrect this as `validation`-module work without first revisiting the product decision at line 133 — that decision is still unmade, this entry only fixes the contradiction, it doesn't answer the underlying question.
3. Whatever the team prioritizes next — the backend and the frontend are both stable and tested; there's no "broken foundation" blocking further work anymore.

---

*Update this file at the end of any session that changes what's true about the project — a stale context.md is worse than none, since the next session (any tool) will trust it.*
